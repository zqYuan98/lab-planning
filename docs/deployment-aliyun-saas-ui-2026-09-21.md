# SaaS 界面优化正式发布记录

状态：**2026-09-21 20:00（北京时间）已上线**，20:00–20:02 运行、公网与浏览器检查通过。

- 正式入口：[天枢实验室部门工作空间](https://lab.notvitamin.com/)
- 发布版本：`saas-ui-20260921T114323Z`
- 源提交：`7ff5ed4475ea2c7218326af2575a99218d8e99e6`
- 上一版本：`weekly-agent-20260921T103834Z`

## 发布内容

本次统一工作清单、月度目标、周计划与概览的优先级和任务类型表达，增加高／中／低优先级及月度计划／临时任务色彩信号；同时收紧灰色文字使用，提高消息、导入、报告编辑器和表格等页面的可读性。全局工作区、登录相关页面、管理员与成员导航、代表性弹窗以及手机布局沿用同一视觉系统。

封存范围相对生产基线为 **25 个修改、8 个新增、0 个删除**。服务端、共享模型、依赖锁、构建配置与部署配置保持原哈希；没有数据库迁移、新环境变量、systemd 或 Nginx 规则变更。

## 构建与验收

- 在 Ubuntu 24.04、Node.js 24.20.0、npm 11.19.0 环境执行 `npm ci`、完整测试与生产构建，**660 / 660 测试通过**。
- 源归档、静态资源和依赖归档均完成逐文件校验；依赖树包含 21,309 个文件和 24 个安全相对符号链接。
- 公网 **49 / 49** 项只读发布检查通过，覆盖 17 个页面与深链、19 个受保护接口、登录和钉钉状态、全部 JS／CSS／SVG／PNG 哈希、安全响应头、新版界面标识、响应式 CSS 与减少动态效果规则。
- 周报模块另执行 **9 / 9** 项匿名只读权限检查，未生成报告、修改设置或写入业务数据。
- 匿名浏览器在 1280 × 720 与 390 × 844 视口完成视觉检查；登录页显示正常，两种视口均无页面横向溢出，检查后关闭临时浏览器。

## 发布过程与数据保护

部署持有发布锁和备份锁，公网先进入维护并排空旧请求，服务按 45 秒宽限正常停止。随后创建并校验独立 SQLite 备份，再原子切换 `/srv/lab-planning/current`；新进程、内部直连和维护期代理验证通过后恢复原入口。

- 数据库备份：`/var/backups/lab-planning/pre-saas-ui-20260921T114323Z.sqlite`
- 备份 SHA-256：`9b55f60c750c04a283dd4323edde598c07be35cb5ba8253ba9e09f40909694a5`
- SQLite 完整性：`ok`；schema 版本：`2`
- 872 条受保护的既有实体保持不变；没有新增或删除实体。仅排除 `nativeRuntime/stream` 的既定连接运行字段。
- `/etc/lab-planning/app.env` 与现网 Nginx 配置前后字节哈希一致；自动周报仍为关闭状态，所有后台队列无活跃项目。

首次隔离准备在激活前发现 Python 安全解包将依赖目录权限收紧为 `0700`。当时正式服务始终运行上一版本；未完成候选目录经发布锁保护后清理，准备脚本补充目录、普通文件和可执行文件的显式权限归一化，再从归档重新解包并完成全量哈希校验。该问题没有进入维护窗口，也没有影响正式数据或公网服务。

## 上线后状态

`lab-planning.service`、Nginx、Cloudflare Tunnel 与备份 timer 均为 `active`；应用进程 `NRestarts=0`。应用、Nginx 与 Cloudflare Tunnel 在激活后均无 error 级日志。公网返回的首页和全部静态资源与封存 Linux 构建哈希一致。

如需回退，可在维护与请求排空后把代码软链接切回 `weekly-agent-20260921T103834Z`，继续使用当前数据库。上线后产生的数据不得用发布前备份覆盖；该备份只用于独立核验和灾难恢复。

完整本机证据保存在 Git 忽略目录 `output/saas-ui-deploy-20260921/`，包括 `assembly.json`、`release.json`、Linux 安装／测试／构建日志、`activation.json`、`postflight.json`、`public-verification.json`、`report-agent-public-smoke.json` 与 `browser-public-verification.json`。
