# 登录页浏览器自适应修复

状态：2026-09-21 15:26:50（北京时间）已上线，版本 `tech-responsive-20260921T072105Z`。正式入口：https://lab.notvitamin.com/ 。

用户在 2552×1274 浏览器看到两侧白边。线上实测登录主容器左侧为 376px、宽度为 1800px；根因是登录页 `<main>` 继承业务页面的 `max-width: 1800px` 和居中外边距。此前验收覆盖至 1440px，且只检查横向溢出，未检查页面是否铺满可用宽度。

修复仅调整 `src/auth-tech.css`：登录画布明确使用 `width: 100%; max-width: none; margin: 0`，取消继承的整体入场动画；主题按钮设置独立层级，避免低高度窗口中登录栏的透明内边距拦截点击。

以线上 `tech-ui-20260921T064409Z` 封存源码为基线，新包 346 个源文件中仅此 CSS 改动。服务端、依赖、业务组件及存储结构与基线一致。

验收脚本为 `output/tech-responsive-deploy-20260921/browser-responsive.mjs`。15种尺寸 × 深浅主题 × 登录/注册，共60个组合：2552×1274、3440×1440、2560×1440、1920×1080、1440×900、1366×768、1024×600、901×700、900×700、700×700、500×700、390×844、320×568、844×390、1280×637。断言包括主容器贴齐左侧、宽度等于可用视口、页面没有横向溢出、背景高度覆盖视口、品牌与主题按钮不碰撞，以及主题/提交/返回按钮滚动后可见且能接收点击。

首次候选检测出 1280×637 时主题按钮被登录栏透明区域遮挡，相关失败证据保留为 `responsive-first-candidate-results.json`。正式结果以同目录的本地和公网验收记录为准。

最终生产构建（TypeScript、Vite）通过。本地与实际公网分别通过全部60个组合；公网加载的 JS/CSS/SVG 哈希与发布包一致，未提交表单、创建会话或写入业务数据。

上线后29项公网检查全部通过；应用、Nginx、隧道和备份定时器均正常，自动重启和错误级别日志为0，数据库完整性为 `ok`。

发布包 SHA-256：`3d8034efb214902b6c0ede3f7a8402619b91cea8ab1786829ea805e50be542ea`。上线前备份为 `/var/backups/lab-planning/pre-tech-responsive-20260921T072105Z.sqlite`，完整性 `ok`；备份 SHA-256 为 `6f1082e7188523edcaa4510d57aa9c174ad4bbd3212360457e4cbbee4e9741e8`。切换前后839条受保护实体保持一致，环境文件与Nginx配置逐字节保留，未进行数据库迁移或恢复。

回退目标为 `tech-ui-20260921T064409Z`，继续使用当前持久数据库。完整证据见发布目录内的 `release.json`、`activation.json`、`responsive-local-results.json`、`responsive-public-results.json`。
