# 月度临时目标与导入操作验证

日期：2026-09-17。

## 范围

- 月度临时目标：成员本人新建、修改、提交，管理员审核发布；按月记录阶段成果，跨月承接保留来源和旧月结果。
- 数据导入：拖入文件/图片、多文件逐个处理、直接粘贴截图、输入格式及大小校验。
- 删除导入批次和单条历史资料：权限、版本、解析并发、共享原文件、事务、去重、正式数据保留及删除后的导出恢复。

## 自动验证

独立验证副本基于原提交 `74d38f2`，仅叠加本轮文件。主工作区同时存在另一项总览页开发，因此未将其尚未完成的文件纳入本轮构建。本轮 16 个实现和测试文件已通过 SHA-256 比对，副本与主工作区一致。

- `npm test`：180 项通过，0 失败，0 跳过。
- `npm run build`：TypeScript 检查和 Vite 生产构建通过；仍有现有主包超过 500 kB 的构建提示。
- `git diff --check`：本轮改动通过。
- 删除后的全量 JSON 导出、恢复验证包含已纠正的历史资料，保留既有计划及审计，不恢复已删除归档；仍拒绝非法人员引用。
- 旧结构化批次两种分叉删除顺序均完成额外复核；同来源重导复用原计划，新增数为零。

## 浏览器验证

使用 Playwright 独立浏览器会话，连接本地内存数据库。图片解析使用本机模拟模型；本轮未调用真实外部模型，也未操作正式数据库。

- 同时拖入 TXT 和 PNG，两个文件分别显示保存成功，批次数从 2 增为 4。
- 拖入图片调用原解析流程，结果进入待校对列表。
- 通过浏览器剪贴板写入合成 PNG，聚焦上传区域后实际按 Ctrl+V，生成 `image.png` 图片批次，批次数增为 5。
- 取消删除保留原批次；确认删除活动批次后，批次数由 5 减为 4，当前详情清空。
- 单条历史资料删除后，历史列表由 2 条减为 1 条，保留另一条资料及原来源批次。
- 成员临时原因空缺时，表单阻止提交；填全后可保存并提交审核，列表显示临时标记，待审核状态不再显示成员编辑/提交入口。
- 管理员完成审核、发布九月 V1，并跨月承接到十月；十月为独立草稿，显示临时和承接标记，截止日为 2026-10-31，保留原临时原因，实际成果为空、状态待交付。

截图保存于 `output/playwright/import-drop-delete.png`、`output/playwright/monthly-temporary-member.png` 和 `output/playwright/monthly-temporary-carry.png`。

本记录描述代码与隔离环境验证。用户随后授权部署，本版已于 2026-09-17 10:45 上线 37 服务器，详见[部署记录](deployment-37-monthly-temporary-import-2026-09-17.md)。
