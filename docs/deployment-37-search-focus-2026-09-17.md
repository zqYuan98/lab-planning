# 37 服务器：全局搜索框焦点修复

2026-09-17 14:44（Asia/Shanghai），在已授权的线上 UI 更新中修复用户截图反馈。

- 地址：http://192.168.0.37:4310/。
- 应用提交：`22747bb248b48246ed49661cc55092e204068cdd`，基于部门概览版本 `60238ec` 及部署文档。
- 当前目录：`/home/yzq/apps/lab-planning/releases/22747bb`。
- 镜像：`lab-planning:22747bb`，运行别名 `lab-planning:intranet`。
- 镜像 ID：`sha256:d2b30deb5ba42b7c76e2d276b619365e73a8952f3f9bb574ee449d569b5e378c`。
- 发布包 SHA-256：`9b6d64a5407a836f964277863cfcb80515faf87d9c8c4dae06f0c7ae4c16e982`。

## 修复与验证

仅在 `src/workspace-shell.css` 添加 9 行：由外层搜索控件统一提供边框及焦点环，清除 Arco 前缀输入内层重复边框、12px 内边距、背景和实际 input 的聚焦阴影。搜索逻辑、服务端、依赖、容器配置和数据库结构均未改动。

- 1665px 桌面、375px 手机：外层 40px，内层由 40px 改为 38px；内层边框 0、阴影 none，无页面横向溢出，外层焦点仍清晰可见。
- 搜索结果下拉、方向键与 Enter 打开匹配月度目标、对话框外的 / 快捷聚焦、清空及 Tab 返回输入框均正常。
- 修复截图保存在 `output/overview-review/search-fixed-desktop.png`、`search-fixed-mobile.png`，采用隔离合成资料。
- 本地构建通过；服务器候选镜像构建及 **190/190 测试通过**。隔离容器的认证、静态资源、SQLite 写入、Word 导出、备份与重启持久化验证通过。
- 正式样式 `/assets/index-BlYDe3H2.css` 返回 200，已检查两条修复规则，资源名与本地验收构建一致。正式登录页浏览器运行错误为空。

## 并发写入与重新发布

首次切换恢复访问后，用户上传文件，新增导入来源、批次和事件，触发旧脚本的全实体哈希相等检查。脚本回切原应用 `60238ec`，继续保留同一个数据卷，没有恢复旧数据库。随后只读逐实体核对：原记录无修改或删除；新增上传及后续解析记录完整保留。

重试以包含新增记录的当前数据库为基线，保留首次备份，另生成 `backups/before-search-22747bb-retry1.sqlite` 和在线备份 `backups/lab-planning-20260917T064358.095414225Z-2474629.sqlite`，完整性校验通过。

发布后全库哈希改作审计信息；运行中导入任务仅在停服前拒绝切换。仍强制检查当前镜像、原数据卷 `lab-planning-37-data`、存储版本 1、数据库完整性和容器健康，避免正常并发操作被误判为数据损失。

最终切换后 `running / healthy`，`integrity_check=ok`；相对重试前备份，新增、修改、删除均为 0。实体 SHA-256 为 `3e44257d5553d3e99a77290aae0f66eb96c79e7582cc98b2072b7b0ebb245297`。最终保留 6 个账号、12 个月度目标、12 个任务、12 条周记录及所有导入资料。

旧镜像 `lab-planning:before-search-22747bb` 和原目录 `releases/60238ec` 保留。服务器 `ops/` 保留名称含 `22747bb` 的构建、验收、两次切换日志与逐实体差异审计；不得用旧备份覆盖新数据。
