# 成员账号生命周期验收（2026-09-17）

## 规则

采用用户确认的规则：无业务关联账号可删除；有业务或历史记录的账号只停用，保留责任归属。当前列表和新增分配隐藏停用成员，历史可主动查看。详见 [成员账号管理](member-accounts.md)。

## 自动验证

- `npm test`：223 项通过，0 项失败。
- `npm run build`：通过；保留现有 Vite 大包提示。
- `git diff --check`：通过。
- 删除测试覆盖权限、姓名和版本校验、当前账号及最后管理员保护、嵌套历史引用、预览后的新增关联、事务回滚、凭据清理、导出恢复和 HTTP 接口。
- 成员筛选测试覆盖停用和未审批账号、历史责任人保留、月度协作目标、历史提报名单。概览测试核对默认统计和主动查看历史的统计一致性。

## 浏览器验收

仅使用合成 SQLite 数据，未修改或删除正式账号。桌面视口 1440×1000，移动视口 375×844。

1. 部门概览默认不显示停用成员唐文澈，任务总数 13；勾选“包含停用成员”后显示该成员，任务总数 14。
2. 成员管理默认显示启用成员，停用成员可在状态筛选中查看。
3. 无业务记录的“测试空账号”可进入删除确认，输入正确姓名前按钮禁用。确认后账号删除，历史任务保留。
4. 唐文澈的删除预览列出计划、任务、周记录和发布记录，均为 1 条；不提供确认删除按钮。
5. 删除预览在移动视口无横向溢出，页面宽 375，弹窗宽 343。
6. 周计划默认隐藏停用成员；切换到该成员有记录的 2026-09-07 周并包含停用成员，可查看原任务“归档历史模型基准资料”。
7. 月度目标默认隐藏停用成员单独负责的目标；主动包含后显示目标和“唐文澈（已停用）”。
8. 编辑该历史月度目标并保存，原负责人仍为 `fixture-user-tang`，原任务保留。验证禁用的负责人选项不会导致提交时丢失历史归属。
9. 浏览器错误日志为空。

截图和本地验收日志保存在主工作区 `output/account-lifecycle-review/`，不包含在应用发布包中。

## 发布

已完成正式切换，服务器隔离测试同样为 223 项通过，数据核对无新增、删除或修改。详见[部署记录](deployment-37-account-lifecycle-2026-09-17.md)。
