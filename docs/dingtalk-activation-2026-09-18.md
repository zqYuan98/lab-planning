# 钉钉应用发布与通知试点接续记录

后续状态：2026-09-19 自检时，通知范围已为 5 人、4 人绑定、2 条平台报告成功；本轮没有修改通知范围。当前能力边界、等待时段和待修项见[9 月 19 日全面自检](dingtalk-self-check-2026-09-19.md)。本文下列内容保留为 9 月 18 日操作记录，不能作为当前唯一试点或唯一绑定的依据。

日期：2026-09-18。本轮在用户要求“检查并且操作一下”后继续执行。

最新状态（18:44）：袁中群已于 **18:26:19.785** 在真实钉钉客户端完成明确绑定；外发已启用，仅袁中群试点。绑定兼容修复及通知格式修复已上线，个人提醒在 **18:43:59** 获钉钉平台受理，**18:44:44** 返回“平台报告成功”。用户实际打开卡片与绑定后新会话免登尚待单独验收。以下前序未绑定描述为各检查时点的历史记录。

## 17:50 现场检查

- BrowserAct 中“天枢实验室”H5 应用已显示“应用发布（已发布）”。可使用范围选择“部分员工”，包含陈圣阳、陈长文、夏小芮、曾其乐、袁中群。此状态已由用户在两轮操作之间完成，本轮没有重复发布或扩大范围。
- 用户已在本轮 BrowserAct 管理系统窗口完成登录，页面显示“袁中群 / 部门管理员”。
- 企业应用凭据已配置；服务器外发开关为 `false`，管理界面外发设置为 `false`。
- 已选试点只有袁中群，发送时段为北京时间 08:00–20:00；无人完成钉钉绑定。
- 服务器应用服务为 active，无运行中的导入解析。投递队列仅有 8 条 skipped，无 pending、sending 或 accepted。

## 本轮操作与后续验证

北京时间 17:56:32 已将服务器 `DINGTALK_NOTIFICATIONS_ENABLED` 开启为 `true`，随后通过管理界面勾选启用并保存。页面已显示 **“钉钉外发已启用”**；试点仍仅袁中群，时段仍为 08:00–20:00，其他四位成员未加入试点。

保存时袁中群仍为“未绑定”。当前等待用户在真实钉钉客户端完成本人身份绑定。普通 Chrome 的网站账号登录不包含钉钉 JSAPI 本人身份，不能替代此步骤。本轮尚未进行真实通知发送或确认真实免登成功。

服务器操作持有部署锁并使用短暂维护窗口，确认旧代理请求排空和无运行中的导入任务后重启。仅修改外发开关的字面值，四项凭据、部署标识及其余环境文件字节保持原样；环境文件仍为 `0600`。原 Nginx 配置与公网入口已恢复，服务 active，代理认证状态与钉钉配置接口均返回 200。

服务器备份与验证目录：`/root/lab-planning-enable-notifications-20260918T095628108203Z/`。本地验证记录为 `server-enable-result-attempt2.jsonl`。首次启用因重启后的即时检查未通过，已成功自动回退；将检查改为先等待应用接口就绪、再核对进程环境后，第二次完整验证通过。首次脱敏输出未保留具体失败分支，因此启动时序是推断原因，不能断言已由原始错误证实。

启用前后均无 pending/sending/accepted 队列，既有 skipped 历史记录不会因本次启用或之后绑定自动补发。

BrowserAct 远程接管功能要求额外 API Key，当前未配置，因此没有生成可用的远程接管链接；用户已直接在本机浏览器完成管理系统登录。

证据目录：`output/dingtalk-config-20260918/`，其中 `publication-status-followup.md` 和 `notifications-before-enabling.md` 为本轮页面记录。页面索引仅对当次 state 有效，不能作为以后重放的固定定位。

## 绑定入口排查

用户确认从电脑钉钉打开应用后跳到了 Chrome/Edge。当前页面只有“我的钉钉身份 / 未绑定”的说明，没有操作按钮，是因为项目仅在识别为钉钉客户端时显示“验证当前钉钉身份”。只有授权码被服务器验证并产生短时待绑定证明后，才出现“确认是本人，绑定账号”。普通网站账号登录不产生该证明。

本轮 BrowserAct 检查的页面 UA 为普通 Chrome；服务器截至 18:06 的最近绑定相关请求亦为普通浏览器，未见授权码兑换或绑定 POST。后台开发管理表单只提供手机/PC 首页地址，没有内置/外部浏览器切换项；未擅改首页或身份验证逻辑。

已根据[钉钉官方 AppLink 文档](https://open.dingtalk.com/document/orgapp/open-h5-micro-application)生成[在钉钉内打开天枢实验室](https://applink.dingtalk.com/page/h5_app_open?appId=5007337208&appType=2&corpId=ding3642d28c19a9b98235c2f4657eb6378f&path=%2Fwork&target=panel&targetDesktop=popupWindow)入口。它使用该内部 H5 应用的 AgentId、企业 CorpId，指定桌面端钉钉独立窗口，返回 `/work` 页面。

BrowserAct 直接打开该唤起链接未在超时前取得 DOMContentLoaded，因此不能宣称钉钉客户端已被成功唤起。已请用户点击入口，并在系统询问时允许“打开钉钉”。待用户确认实际客户端页面与本人绑定结果后，再验收免登和真实通知；本轮未伪造客户端环境、未代填钉钉用户身份、未直接改数据库绑定。

## 18:15 绑定落库核对

用户反馈已看到验证按钮并完成绑定，但 BrowserAct 刷新“我的工作与消息”和“通知设置”后，袁中群仍显示未绑定。服务器只读核对于北京时间 18:15:23 完成：钉钉绑定总数为 0，绑定审计为空，也没有绑定到其他系统账号。

访问日志显示真实 PC 钉钉 8.5.5 客户端于 18:10:31、18:10:59 调用 `POST /api/auth/dingtalk/exchange`，均返回 200；18:11:25、18:11:54 读取绑定状态返回 200。最近三小时没有 `POST /api/dingtalk/bind` 或解绑请求。因此 AppLink 已使客户端到达身份验证步骤，但最终确认绑定尚未到达服务器，不能认定绑定、免登或真实投递已完成。

已请用户在真实钉钉窗口重新验证（验证凭证有效期 5 分钟），核对本人两个账号并点击“确认是本人，绑定账号”，以绿色“已绑定”为完成标志。外发开关仍已启用，试点仍仅袁中群，未发送测试消息或通知其他成员。

## 18:26 本人绑定与 18:31 兼容修复

用户随后报告没有确认按钮，并再次反馈从普通网址打开仍会跳到外部浏览器。提供了上面的官方 AppLink 入口。服务器 18:20:04 的钉钉验证与 18:20:18 读取绑定状态间隔只有 14 秒，不能将缺按钮一概归于 5 分钟过期或用户没有点击。

代码与隔离测试发现：旧验证响应同时发送两条同名 `Set-Cookie`，先清除后写入新验证凭证；模拟只保留第一条的客户端时，验证返回 200 但随后没有待绑定状态。HTTP Cookie 标准建议同一响应不要重复设置同名 Cookie（[RFC 6265 §4.1.1](https://www.rfc-editor.org/rfc/rfc6265#section-4.1.1)）。这证明存在兼容缺陷，但没有实际客户端响应头证据，不能认定它是此次唯一根因。

修复后，验证成功只发一条新凭证，验证失败或已绑定免登只发一条清除凭证；服务器仍立即撤销旧证明，保留过期、企业/账号冲突校验和明确确认。前端防止旧查询响应覆盖新绑定状态，免登成功后重新读取绑定，状态未保留时明确报错。只修改 `server/dingtalk-routes.ts`、`tests/dingtalk-identity.test.ts` 和 `src/components/DingTalkBinding.tsx` 三个源码/测试文件。

新回归测试先在旧实现上以 `2 !== 1` 失败，再验证修复；本地身份与适配器 19/19、其他认证与通知测试 38/38 通过（两组含部分重复适配器测试），TypeScript 与生产构建通过，独立审阅无阻塞问题。服务器再次通过身份测试和生产构建。

部署于北京时间 **18:31:40.966** 完成：

- 新版本：`/srv/lab-planning/releases/binding-fix-20260918T103000Z`，基于线上旧版本独立复制，只替换核对过新旧 SHA-256 的上述三文件并重新构建。
- 原版本：`/srv/lab-planning/releases/dingtalk-20260918T070350Z`。
- 证据与数据库备份：`/root/lab-planning-binding-fix-20260918T103022246240Z/`；备份文件 `database.before.sqlite` 已通过完整性检查，SHA-256 为 `23d0403bce0f609c15067269425cfc397f54ae43894ed18ee1bcd987522a756a`。
- 部署持锁、维护排空、确认无运行中导入和待发送/发送中通知，备份后切换代码；未恢复旧数据库，环境文件字节未变，外发与试点设置保留。服务 active，原入口恢复，代理 `/work` 与新构建一致。
- BrowserAct 已加载新脚本 `index-DvwHDA15.js`，原登录仍可用。

上线后页面和只读数据库复核发现：袁中群已于 **18:26:19.785** 保存唯一钉钉绑定及绑定审计。真实 PC 钉钉 8.5.5 在 18:26:17 验证返回 200、18:26:19 提交绑定返回 200。此绑定早于补丁上线，不能归因于补丁。18:33:13 前尚无绑定后的新免登兑换请求，SSO 未据此宣称已实测。

## 本人通知验收

BrowserAct 中“通知设置”显示外发已启用，仅袁中群勾选且已绑定，其余成员未加入试点。18:33:22 对袁中群现有的“2026-09 月度计划已发布”待确认消息点击一次“提醒本人确认”，生成“请确认工作安排”个人提醒。未创建虚构任务，未替用户确认原安排，未向其他成员发送通知。新提醒最初显示待发送，后续以平台回执和用户实际打开分别验收。

首次发送在 18:33:25 被明确拒绝，原 worker 只保留泛化错误，无法倒推原始错误码。后台再次核对 `qyapi_base`（含异步发送工作通知）已默认开通，未新增机器人能力或扩大成员范围。

[钉钉官方消息通知类型](https://open.dingtalk.com/document/orgapp/message-types-and-data-format)明确链接消息的 `link.picUrl` 必填且需媒体文件标识；旧实现发送空值不符合规范。改为无需图片的整体跳转 `action_card`，保留标题、正文、原同源事项入口，按钮“查看工作安排”。正文按纯文本转义 Markdown，完整 `msg` JSON 仍限制为 2048 UTF-8 字节，URL 不截断并遵守 500 字符限制。仅从严格匹配的适配器错误中保留数字错误码，不记录任意平台错误文字、URL 或凭据，未知结果仍不重发。首次失败原因没有原始错误码证据，不断言空图片是唯一原因。

本地适配器 9/9、通知测试 21/21 和 TypeScript 通过；独立审阅无阻塞问题。服务器再次执行适配器、通知、worker 守卫测试及完整生产构建，均通过。北京时间 **18:43:30.370** 已上线：

- 当前版本：`/srv/lab-planning/releases/message-fix-20260918T104200Z`，基于前述绑定修复版本，只替换 `server/dingtalk.ts`、`server/notification-worker.ts` 及两份对应测试。
- 证据目录：`/root/lab-planning-message-fix-20260918T104209839645Z/`。
- 最新停服一致性备份：该目录内 `database.before.sqlite`，完整性检查通过，SHA-256 为 `9382faacca55aee850e7719c2b99c7ee529626da8c2aeed9726a8a820c6f0f0c`。
- 环境文件字节、账号数据和已有绑定保留；未恢复旧数据库，服务 active，原入口恢复。

更新后只对袁中群这条明确失败的既有投递点击了一次“重试明确失败的投递”，没有新建第二条提醒或重发未知结果，后续状态继续核验。

18:43:59，此提醒变为“平台已受理”，没有再被发送接口拒收。已请用户查看钉钉“天枢实验室”的工作通知并点击“查看工作安排”；平台最终回执、用户实际看到和点击打开分别确认，不将受理状态当成送达或已读。

18:44:44，刷新后此提醒显示 **“平台报告成功”**，系统查看仍为“尚未查看”。后端只在平台返回的已读/未读接收人名单明确包含该绑定成员时记为成功，因此已验证实际平台发送回执。没有把此状态当作用户已阅读，也没有替用户确认任何工作义务。仍等待用户回答是否已收到、点击后在钉钉内还是外部浏览器打开。完整配置、绑定与通知检查至此有平台成功证据。
