# 清爽现代 B 端 UI 重构正式发布记录

状态：**2026-09-22 09:54（北京时间）已上线**，生产状态、公网资源与匿名浏览器检查通过。

- 正式入口：[天枢实验室部门工作空间](https://lab.notvitamin.com/)
- 发布版本：`ui-refresh-20260922T014640Z`
- 源提交：`e3b2d5bfffe3a9aa66f13da17999cfc69c259b3e`
- 上一版本：`saas-ui-20260921T114323Z`

## 发布内容

本次只重构 UI：统一全局工作区的导航、画布、表面、按钮、表单、文字色阶和响应式节奏；重点重做部门概览与我的工作清单的页头、指标卡、操作区、选项卡、筛选器和任务/表格区域。任务优先级、逾期、月度计划与临时任务继续使用现有语义和数据，只调整颜色、图标、标签、边线与排版表达。

候选版本相对正式生产基线为 **10 个修改、2 个新增、0 个删除**。新增内容仅为设计与验收文档；`server/`、`shared/`、API、数据库、依赖锁、构建配置和部署配置均保持原生产哈希。

## 构建与验收

- Windows 本地类型检查、生产构建与 `git diff --check` 通过；完整测试 **660 / 660** 通过。
- 在 Ubuntu 24.04、Node.js 24.20.0、npm 11.19.0 环境重新执行 `npm ci`、完整测试和生产构建，**660 / 660** 再次通过。
- 396 个候选源文件、全部构建资源，以及 21,309 个依赖文件和 24 个安全相对符号链接完成逐文件校验。
- 公网 **49 / 49** 项只读检查通过，覆盖主要页面与深链、受保护接口、登录与钉钉状态、安全响应头、新版界面标识、响应式规则、减少动态效果规则及全部部署资源哈希。
- 本地隔离 fixture 已验收 2048、1440、390 与 320px；核心两页在 390/320 下为单列指标、无根级横向溢出，主要移动控件为 44–46px，KPI 说明为 12px。
- 正式站点匿名浏览器在 1280 × 720 与 390 × 844 视口显示登录页正常，均无横向溢出；临时浏览器已关闭。

## 发布过程与数据保护

上线前只读检查确认原版本、服务、锁、磁盘、队列和 SQLite 状态正常。候选版本在独立目录解包，源文件、构建资源和依赖清单校验通过后，发布过程进入维护、排空请求、正常停止服务，创建并校验独立 SQLite 备份，再原子切换当前版本链接并恢复入口。

- 数据库备份：`/var/backups/lab-planning/pre-ui-refresh-20260922T014640Z.sqlite`
- 备份 SHA-256：`25f6de73e17efb15bbe34a199572681e632d9b5ebb7e6cf500b6d888a329dd34`
- SQLite 完整性：`ok`；schema 版本：`2`
- 881 条受保护业务实体保持不变；新增 `0`，删除 `0`
- `/etc/lab-planning/app.env` 与 Nginx 配置前后哈希一致
- 自动周报仍为关闭状态，后台队列无活跃项目
- 发布过程未恢复旧数据库；回退路径只切换代码

## 上线后状态

`lab-planning.service`、Nginx、Cloudflare Tunnel 与备份 timer 均为 `active`；应用进程 `NRestarts=0`。应用、Nginx 与 Cloudflare Tunnel 在激活后均无 error 级日志。公网首页和全部静态资源与封存构建哈希一致。

完整发布证据保存在 Git 忽略目录 `output/ui-refresh-deploy-20260922/`，包括 `assembly.json`、`release.json`、Linux 安装/测试/构建日志、`activation.json`、`postflight.json`、`public-verification.json` 和 `browser-public-verification.json`。
