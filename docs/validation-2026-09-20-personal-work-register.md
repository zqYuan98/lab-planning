# 我的工作清单验证记录

## 完成范围

新增本人全周期工作清单与领导交办视图；多行快速录入、可未知截止日期、来源与交办信息、任务编辑、优先级与预计剩余投入、进展/待反馈/支持与决策信息。沿用原 Task 排入周计划，支持固定内容的汇报预览、CSV 导出与打印 PDF。管理者的个人清单也只显示本人事项。

新增字段通过现有业务 JSON、CSV 和 Excel 导入导出保留。既有 API、静默历史导入和督办试点开关行为保持兼容；从清单更新当前进展计入现有有效进展时间线。由并行钉钉任务同步补齐其变化字段迁移白名单与周报显示标签。

## 自动化检查

- `npm test`：445/445 通过，2026-09-20 最终全工作区结果（包括并行任务已落盘的新增测试）。
- `npm run build`：通过。保留现有大包体提示，未增加依赖。
- 本次领域测试 11 项、投影/快照/CSV 测试 12 项、HTTP/导航测试 2 项、协作进展基线测试 1 项。
- 权限、批量事务回滚、请求重试幂等与冲突、空日期、版本冲突、月度重新关联、迁移往返、跨年/跨周去重、总体与周状态独立、未知日期不误报逾期、表格公式保护均覆盖。
- 对源文件运行 `git diff --check`，无空白错误。

## 浏览器验证

使用 `.tools/work-register/preview.sqlite` 假数据、独立 4327 服务。未连接或写入正式环境，未启动通知和调度 worker。

1. 1440 × 1050 桌面、390 × 844 手机正常渲染；手机 documentElement.scrollWidth 为 390，无横向溢出，无页面运行错误。
2. 从 UI 批量记录「修订年度规划预算测算」「准备领导汇报一页摘要」，交办人为演示称呼，截止留空。预览拆成两条，保存后显示待确认。
3. 编辑预算事项的交付、进展、优先级、预计投入及决策信息；点击安排本周，原任务已被选中，保存为周草稿。数据库核对仅一件原 Task，周记录复用同一编号，未自动提交整份周报。
4. 仅在浏览器模拟北京时间进入 2026-09-21 并触发 focus，本周推进从 2 变 0、待安排从 5 变 7；全部在手仍是 7。刷新页面后恢复真实时钟，系统时间未修改。
5. 故意中断保存后的 bootstrap 刷新：已保存的新事项仍显示，排周按钮禁用并提示刷新。解除中断后重试刷新恢复排周，数据库确认任务仅创建一次。
6. 预览含未排期和截止待确认事项，包含本人来源、交付、进展、下一步和决策信息；不含其他成员任务。
7. 实际 CSV 下载成功：独立 Playwright context 开启 acceptDownloads 后，`download.failure() === null`，2172 字节、UTF-8 BOM `efbbbf`，中文名称与 7 条当时预览数据正确。agent-browser 下载命令的取消属于工具下载处理，不是页面 Blob 下载失败。
8. PDF 打印共 4 页，逐页检查文本与渲染，全部事项可见，侧栏和预览按钮不进入 PDF。修正 html 根背景后，页面末尾保持白底。

本地证据：`.tools/work-register/desktop.png`、`mobile.png`、`report-preview.png`、`report-final.pdf`、`final-tests.log`、`final-build.log`；真实下载文件为 `output/playwright/register-playwright-download.csv`。这些为本地忽略的演示产物，不含正式业务资料。

## 交付状态

代码在当前工作区完成，设计与实施记录已归档。保留所有此前及并行任务的修改，不对混有其他改动的源文件作整文件提交。本次未部署到正式服务器。
