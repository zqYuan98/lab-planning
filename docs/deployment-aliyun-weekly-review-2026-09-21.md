# 周计划审核与管理员留痕删除发布记录

状态：**2026-09-21 16:20:53（北京时间）已完成上线**，16:21 公网与运行检查通过。

- 正式入口：[每周执行](https://lab.notvitamin.com/work?view=weekly)。
- 本次版本：`weekly-review-20260921T080122Z`。
- 前一版本：`tech-responsive-20260921T072105Z`。
- 证据目录：`output/weekly-review-deploy-20260921/`。

## 上线范围与实际生效时间

管理员可填写原因删除录错的单条周安排，保留原任务、历史提交及报告；删除后可补关联月度常规或临时目标，再沿用原任务在同一周建立新的安排。删除功能上线即生效。

成员填写下周计划后，核对并提交整份计划；管理员针对指定提交版本通过或填写原因退回。修改计划承诺后需重新提交审核，日常进展更新不重审。审核与提交时效分别记录，历史批准版本保留。

生产初始化写入的审核生效周期为 **2026-09-28**，首次覆盖的下周计划从 **2026-10-05** 开始。历史计划不补审，既有提报开关和生效窗口保持原值。

发布包从当前正式响应式版本的封存源码组装，共 356 个源码文件，仅包含经核对的 44 个修改和 10 个新增文件，其余 302 个基线文件字节一致。保留现有反馈功能、科技风界面及响应式修复；没有直接发布整个工作区的其他改动。

发布归档 SHA-256：`14884d18bf27339a2985cfa335b74ef07e5d70ebd42a4f6cc0831c9ffe58fad8`。

## 验证结果

- 实际封存发布包在 WSL Ubuntu、Node.js 24.20.0 下 582/582 测试通过，TypeScript 与 Vite 生产构建通过，构建前后源码哈希一致。
- 初始化数据守卫 25/25 测试、失败回退及精确逆操作 11/11 测试通过。
- 实际封存代码在本机内存库、生产 CSP 下通过 14 项浏览器验收：退回、成员修改重提、通过、删除、补月度关联、同任务同周重建、返回正确提报周期、成员权限和手机布局。保存 8 张截图，两会话页面错误、控制台错误及 CSP 违规均为 0。
- 公网 31/31 只读检查通过：入口及周计划深链、鉴权和请求定位编号、全部 JS/CSS/SVG 哈希、原有与新增功能标识。
- 公网真实浏览器匿名打开周计划深链，桌面 1264px 与手机 390px 均正常显示登录入口，无横向溢出，页面错误及控制台错误为 0。未登录或提交生产表单，验收后关闭浏览器与本地内存验收服务。
- 生产服务器复用与前版完全相同的依赖文件及依赖目录，没有执行安装、构建或完整测试。主 JS 约 910 kB，低于既有健康检查的 2 MiB 单资源上限；Vite 的 500 kB 建议阈值提示仍存在。

## 数据保护与运行结果

部署持有发布锁与备份锁，入口进入维护后排空请求，正常停止旧服务并生成独立备份，再原子切换代码。初始化后逐项核对 855 条受保护实体，仅一条 `weeklyRules` 发生预期审核规则初始化变化。既有 Stream 连接运行状态按指定字段处理。

- 当前软链接：`/srv/lab-planning/current` → `/srv/lab-planning/releases/weekly-review-20260921T080122Z`。
- 数据库继续使用 `/var/lib/lab-planning/lab-planning.sqlite`，完整性 `ok`，存储版本仍为 2。
- 上线前备份：`/var/backups/lab-planning/pre-weekly-review-20260921T080122Z.sqlite`，完整性 `ok`。
- 备份 SHA-256：`64487ee9aeb3f43fe9ba9f7b5da42b7deaa69f0574152b50b5b6780d0b35eab6`。
- 6 个账号、16 个月度目标、29 个任务、30 条周记录、7 份历史提报、5 个外部身份绑定全部保留。
- 环境文件和 Nginx 配置字节一致，既有通知渠道配置保留；未发送测试通知，未将合成验收数据写入生产，未下载正式数据库。
- 应用、Nginx、Cloudflare 隧道与备份 timer 均为 active；激活后自动重启 0、错误级别日志 0。检查时应用约 139 MiB，系统可用内存约 995 MiB，swap 使用 0。

## 回退边界

本次切换成功，未触发回退，也未恢复数据库备份。旧版本不识别周安排删除标记和计划审核状态，因此恢复公网后不能直接切回旧版，也不能用上线前备份覆盖后续真实业务；如需修复，应发布兼容这些数据的新版本。

部署脚本仅在入口尚未恢复、没有业务写入且校验通过时，允许在事务中精确撤销本次初始化产生的规则变化，再恢复旧版。该路径包含状态复核和条件更新，保留运行状态，已由独立测试验证。

完整证据见 `assembly.json`、`release.json`、`linux-tests.log`、`linux-build.log`、`policy-guard-tests.log`、`activation-inverse-tests.log`、`browser-release-results.json`、`preflight.json`、`prepared.json`、`activation.json`、`postflight.json` 和 `public-verification.json`。
