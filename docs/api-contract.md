# API contract

Shared types in `shared/types.ts` and the domain contracts under `shared/` are authoritative. JSON responses return the object/array directly. Errors return `{error:string,requestId:string,code?:string,fieldErrors?:Record<string,string>}`; the requestId identifies the server trace, separately from a client's business command ID. Authentication uses HttpOnly same-origin cookie. All mutations (except login/setup/register) require authenticated identity and application/json. Entity update requires version; mismatches return 409. Use strings for IDs, ISO timestamps, YYYY-MM months and YYYY-MM-DD dates. No automatic company data import.

- GET /api/auth/status -> {initialized:boolean}; POST /api/auth/setup {name,email,password} -> User first manager; POST /api/auth/login {email,password}->User; POST /api/auth/logout; GET /api/auth/me -> User.
- GET /api/workspace -> current identity/configuration shell, including `operationEpoch` and `accessScopeVersion`. Collections use dedicated paged workspace reads; the production `/api/bootstrap` route is retired (404). See [workspace data paths](workspace-data-paths.md). Former membership only exposes authorized historical snapshots, never current write/candidate access; observers use independent explicit grants.
- POST /api/users {name,email,password,position,role}; PATCH /api/users/:id {version,name?,position?,active?,role?,password?} manager only. Roles are `member`, `manager`, or `observer`; only managers create observers. Cannot remove/demote last active manager.
- GET /api/users/:id/deletion-preview -> {user:User,canDelete:boolean,blockers:{key:string,label:string,count:number}[],retainedHistory:true}; active manager only. Lists business, historical, import and frozen-snapshot references, self/last-manager protection. Safe user only, never credentials.
- DELETE /api/users/:id {version,confirmName} -> {deleted:true,id}; active manager only. Requires current exact name and version, transactionally rechecks all preview conditions, removes the unreferenced account and its sessions/integration tokens. Business and frozen history are never cascaded. Account lifecycle audits remain with safe identity snapshots and stay excluded from business migration packets. Any business/history association returns 409; use deactivation instead.
- POST /api/projects {name,code,description,ownerId}; PATCH /api/projects/:id {version,...}. Manager only; archive instead of delete.
- POST /api/annual-goals {title,year,target,progress,progressMode?,description,ownerId}; PATCH /api/annual-goals/:id {version,...}. Manager only. `progressMode` is manual (legacy default) or linked. Optional `MonthlyPlan.annualGoalId` must match its month year. Automatic progress counts accepted carry/merge chains once, with no denominator represented as null; the manual value remains stored. See [business R4 contracts](business-r4.md).
- POST /api/plans {month,title,projectId?,category,ownerId?,collaboratorIds?,expectedOutcome,acceptanceCriteria,dueDate,priority?,isTemporary?,temporaryReason?,workSource?,assignedBy?,assignedOn?} -> MonthlyPlan; creates draft. Ordinary goals require manager. Members may create their own temporary goal with isTemporary:true and a nonempty temporaryReason. Due date stays within month; cross-month carry is manager-only. This ordinary endpoint rejects nonempty `sourcePlanId`; source and merge relationships are server-managed by their dedicated commands and are not in its writable field whitelist.
- PATCH /api/plans/:id {version,...editable fields} -> MonthlyPlan. Manager, or owner of a temporary draft/returned goal. Members cannot change goal type/ownership or edit submitted goals. Manager may edit submitted/approved; published changes require nonempty reason and create new publication snapshot. Preserve audit before/after; do not overwrite original submissions. Published owner/collaborator changes cannot orphan assigned tasks.
- POST /api/plans/:id/submit {version}; POST /api/plans/:id/review {version,decision:'approve'|'return',comment}; POST /api/months/:month/publish {planIds:string[],reason?}. Submit allows manager or temporary goal owner. Review and publication require manager. Manager publishes approved items, revision snapshot contains all published month plans. Publication does not accept results.
- POST /api/plans/merge {planIds:string[],title:string,reason:string}. Manager combines at least two submitted/approved proposals in same month and same project (or same department category), before tasks exist. New approved plan preserves owner-specific source outcomes/criteria and all owners as participants; mergedFromIds links originals, sources become merged with mergedIntoId. No source deletion; merged sources cannot be independently published or edited. Manager can edit merged target then publish.
- POST /api/plans/:id/result {version,actualOutcome,acceptanceStatus:'submitted'|'accepted'|'not_completed',acceptanceNote?}. Owner submits actual results, manager confirms accepted/not_completed. Submitted/accepted require a nonblank outcome. Not_completed requires a nonblank acceptanceNote explaining the reason; outcome may be empty. Historical missing reasons remain readable; a new not_completed confirmation must supply one. A missing or invalid reason returns 400 with an acceptanceNote field error.
- GET /api/plans/:id/history -> AuditEvent[]. Authorized member/manager.
- POST /api/plans/:id/carry {requestId,sourceVersion,month,dueDate,reason}, with `X-Operation-Epoch` from bootstrap -> MonthlyPlan. Active manager only; creates a draft in a later month, preserving participants, workSource/assignedBy/assignedOn and temporary goal semantics. Source history, result and publication snapshots stay unchanged. The actor-scoped requestId is 16–100 letters, digits, underscores or hyphens; retain the same ID and exact payload after an unknown result. A matching receipt returns the currently authorized target (possibly already published or merged) before checking the old source version, without another write or audit. Changed content under one ID returns 409 `IDEMPOTENCY_MISMATCH`; a new stale-source command returns 409 `SOURCE_VERSION_CONFLICT`; a missing prior target returns 409 `CARRY_TARGET_MISSING`. Missing/stale epoch, including one found only in the body, returns 409 `OPERATION_CONTEXT_CHANGED`. Target creation, command receipt and audit are atomic. A deliberate separate carry uses a fresh ID after inspecting existing targets.
- DELETE /api/imports/:id {version} -> {ok:true,deletedHistoryCount:number}; batch owner or manager. Removes batch and its archived historical records; shared source files remain until unused. Formal plans/tasks/weekly records and reports are preserved. Active analysis or stale version returns 409.
- DELETE /api/imports/history/:id {version} -> {ok:true}; manager, importer or record owner. Removes only the archived record and preserves deduplication metadata.
- POST /api/tasks {title,monthlyPlanId?,ownerId?,description,dueDate,isTemporary?,temporaryReason?} -> Task. Bound plan must be own/collaborating; nonmanager owner is always the authenticated actor. Temporary needs reason; formal task requires monthly plan. Weekly draft allowed before plan publication, submission gate below.
- PATCH /api/tasks/:id {version,title?,description?,dueDate?,status?}. Owner/manager.
- POST /api/tasks/:id/relink {version,monthlyPlanId,reason} -> Task. Manager only, target published; preserves task identity and prior weekly monthlyPlanId snapshots.
- POST /api/tasks/:id/cancel {version,reason} -> Task with `cancellation:{cancelledAt,cancelledBy,reason}`. Manager only, current version and nonblank reason required. Any undeleted weekly record (including drafts and other weeks) prevents cancellation with 409. Preserves task status, progress, deleted weekly records, frozen submissions/reports and audit; closes pending task followups, deadline requests, tracking and blockers. Cancelled tasks leave ordinary bootstrap, task totals, work registers, search and actionable notifications; full exports retain them. Further edits, relinking, scheduling and imported rescheduling are rejected. Repeated cancellation returns 409; there is no automatic cancellation after weekly deletion and no restore endpoint.
- POST /api/weekly-records {taskId,weekStart,commitment,actualOutcome?,evidenceUrl?,blocker?,nextAction?,status?,submitted?} -> WeeklyRecord. Unique (taskId,weekStart) among non-deleted rows; owner=task owner; normalize Monday. submitted=true requires published monthly link or explicit temporary task, and does not bypass applicable plan review. done requires actualOutcome; blocked/not_done require blocker. Evidence URL http/https only. Approval and deletion metadata are server-managed.
- PATCH /api/weekly-records/:id {version,...content/status/submitted fields,completeTask?:boolean,taskVersion?:number}; cannot change task/week/owner. Ordinary weekly completion does not change the overall Task or accept the month. Explicit `completeTask:true` requires final weekly status `done`, a nonempty actualOutcome and the current linked taskVersion; it copies actualOutcome into the task completionNote and completes the task in the same transaction, including existing collaboration lifecycle hooks. Already completed tasks retain a valid original explanation; a historical done task with a missing completionNote can be explicitly repaired by this same dual-version command. Stale task/weekly versions return 409 without partial writes.
- POST /api/weekly-records/:id/carry {weekStart,commitment?} -> WeeklyRecord. Same task, next week new planned/draft record; no inherited outcome/status. Record original monthly link, unless task was relinked.
- DELETE /api/weekly-records/:id {version,reason} -> WeeklyRecord with `deletion:{deletedAt,deletedBy,reason}`. Manager only; reason required. Transactionally checks version and writes the tombstone plus before/after audit. Repeated deletion or stale version returns 409; deleted rows cannot be edited or carried. Retains the Task and its completion state, monthly goal, other weeks, progress, submission receipts and frozen reports. Closes this row's pending followups/blocker support and invalidates its pending acknowledgement obligations without clearing historical responses or unrelated task followups. The same Task/week may receive a new record with a new ID; no undelete endpoint. For a corrected monthly association, relink the retained Task first, then create the replacement row under the existing publication/participation/month-overlap gates.
- GET /api/reports; POST /api/reports {type,period} -> Report. Manager only. Weekly period is Monday date; monthly period YYYY-MM. Full immutable source snapshot, metrics independent of narrative/TOP picks; repeated generation creates revision.
- PATCH /api/reports/:id {version,narrative,title?}; POST /api/reports/:id/finalize {version}; POST /api/reports/:id/polish {version}; GET /api/reports/:id/export?format=docx|md. Manager only. Polish only when configured, never fabricate facts, no stats mutation; finalized immutable.
- GET /api/report-schedule; PUT /api/report-schedule {version,enabled,weeklyDay,weeklyTime,monthlyDay,monthlyTime}. weeklyDay 1..7; monthlyDay 0=last day else 1..28. Default disabled. Asia/Shanghai; durable per-period idempotency for scheduled draft generation, no external notifications.

## Base work validation and response ordering (2026-09-22)

Task and weekly rules apply even when collaboration/reminder features are disabled. A task reported as done requires `completionNote`; blocked requires `blockerReason`, `blockerImpact` and `supportNeeded`. A weekly record reported as done requires `actualOutcome`; blocked/not_done require `blocker`; formal blocked submission also requires `blockerImpact` and `supportNeeded`. Validation returns 400 `WORK_VALIDATION_FAILED` with fieldErrors and leaves versions, audits and notifications unchanged. Task status remains independent of weekly status and monthly acceptance; cancellation continues to use its separate metadata.

Legacy incomplete records remain readable and can receive unrelated edits. Explicit status/evidence edits or formal submissions revalidate the final evidence. Trusted import/restore compatibility comes only from a server-created context; an entity's `importSource` field is not a bypass for later human reports.

After a successful mutation, the client applies the authorized returned entities immediately and invalidates earlier reads. A successful complete list controls membership: removed or no-longer-visible entities disappear. A lower version in the same visibility scope cannot replace a newer entity; narrowed historical/reference projections replace formerly broader content. Identity, role, accessScopeVersion or operationEpoch changes invalidate in-flight responses and old completion callbacks. If the write succeeded but a later GET fails, recovery retries reads only and prevents resubmitting the saved command.

`operationContexts` and monthly carry command receipts are runtime state excluded from business JSON transfer. A successful business restore that actually writes data rotates the epoch in the same transaction; a no-op or rollback leaves it unchanged. Whole-database backup includes the epoch: after restoring, rolling back or cloning a backup, stop the app and run `npm run reset-operation-context -- <restored-database-path>` with the new application version before serving requests. Ordinary restarts preserve the epoch. The client locks both sourceVersion and epoch in editable carry drafts and unresolved commands; after a context change it must reload and explicitly inspect existing targets before preparing a new command.

## Imported temporary work

- Import rows keep `kind:'monthly'|'weekly'`. Optional `isTemporary:boolean` and `temporaryReason:string` describe temporary work independently of the destination period; there is no separate `temporary` kind. A missing flag is compatible with legacy rows and does not classify unlinked imported work as temporary. Non-boolean flags are rejected.
- `POST /api/imports/structured`, `PATCH /api/imports/:id` and historical corrections accept these fields. `GET /api/v1/schema` advertises them. `history` may retain an incomplete reason as source material; writing temporary work into `draft` or `existing` requires a nonempty reason. Temporary weekly work cannot also link a monthly goal (`monthlyPlanId` or `linkedRowId`).
- Members may import their own temporary monthly goals as drafts, with normal draft requirements and monthly review/publication afterward. Ordinary monthly drafts remain manager-only. `existing` still requires manager confirmation; members may request confirmation, including for temporary work.
- Business JSON export/restore preserves these optional fields in archived rows and their audit snapshots, along with temporary goal/task fields. Legacy packages without them remain valid. CSV/XLSX retain them in the archived `row` JSON cell and audit snapshot cells; temporary goal/task fields have their own columns. No business packet or database schema version change is needed for these optional JSON properties.

## Backend integration

### Collaboration and native channels (2026-09-20)

Types and input contracts are in `shared/collaboration.ts`, `shared/collaboration-notifications.ts`, and `shared/native-actions.ts`. All routes below are under `/api`, require an active authenticated session and retain same-origin JSON write guards. Business commands use `requestId` plus current versions: exact retries reuse the result; conflicting payloads or stale previews return 409. Manager routes enforce the role in the router or service.

- GET `/collaboration`, `/collaboration/tasks/:id`, `/work-risks`: permission-scoped work, tracking, followups, risks and personal digests. GET `/collaboration/resolve/:type/:id` resolves a task/followup/deadline request only after current task access succeeds.
- GET/PUT `/collaboration/settings` (manager): feature flags, pilot users, explicit manager routing, workday overrides and thresholds. Initial reads do not write defaults. GET `/collaboration/rules-preview?at=<ISO>` (manager) is read-only and does not consume reminder quotas.
- PUT `/collaboration/preferences` `{requestId,version,memberActionsEnabled}` changes only the current user's optional action summary. It does not remove required followup obligations.
- POST `/tasks/:id/progress` appends progress and validated task/weekly changes. Ordinary saving does not respond to a followup or formally submit a week. Proxy progress requires a reason and does not reset the owner's meaningful-progress clock.
- POST `/tasks/:id/followups`, PUT `/followups/:id`, POST `/followups/:id/close` (manager): one open request per task; changes/closure retain reasons. POST `/followups/:id/respond` is an explicit owner response with request/task/optional weekly versions and progress payload.
- POST `/followups/preview`, `/followups/batch` (manager): 1–100 unique tasks, frozen requirement/deadline, recipient grouping and shared daily quotas; commit requires the actor/settings/task/tracking/request-bound preview token and explicit enrollment when needed.
- PUT `/tasks/:id/tracking` (manager): explicit enrollment, pause/review/resume or close with task/tracking versions. Resuming starts a new observation baseline without rewriting deadlines or past progress.
- POST `/tasks/:id/deadline-requests` (owner), POST `/deadline-requests/:id/decide` (manager): retain original deadline until approval; compare request and deadline versions. POST `/blockers/:id/handle` is now a compatibility adapter to the base support service: assigned coordinators may record/respond, while deferral and management closure require a manager; see the phase-two contract below.
- GET `/digests/:id`: recipient-only digest, filtered against current ownership/access. Rendered lines distinguish historical facts from current obligation status; no stored item is rewritten by reading. Notification previews use the same projection; actual sends also omit resolved actionable items.
- Weekly duty views include an optional progress-event manifest. Formal submission validates the reviewed IDs; new relevant progress after the preview returns 409. Existing receipts and first punctual/late facts are retained.
- GET `/notification-diagnostics` (manager): cursor-based full-history delivery filtering/counts and redacted runtime/backup diagnostics.
- GET/PUT `/native/settings`, POST `/native/identities/:userId/verify`, GET `/native/operations`: permission-scoped status and manager configuration. Capability enablement requires configured server parameters and recorded real tenant verification; defaults remain disabled.
- POST `/native/links/:id/reconcile`, `/recreate`, `/delete` (manager) queues controlled native work; explicit recreation is restricted to verified external-missing objects. POST `/native/intents` and `/native/intents/:id/confirm` preview then confirm one bound action using a short-lived single-use token.

Official Stream callbacks use the configured SDK connection and durable inbox rather than an unauthenticated public business mutation endpoint. External network work occurs outside Store transactions. Unknown native acceptance blocks same-obligation retries and channel fallbacks. See [operations](dingtalk-collaboration-operations.md) and [native protocol/tenant requirements](validation-2026-09-20-dingtalk-native.md).

server/store.ts exports Store: constructor(path:string), get<T>(collection,id):T|undefined, list<T>(collection):T[], insert<T extends Entity>(collection,input:Omit<T,keyof Entity>&Partial<Entity>):T, update<T extends Entity>(collection,id,expectedVersion:number,patch:Partial<T>):T, transaction<T>(fn:()=>T):T, close(). New entity ID/time/version are assigned by store; revision starts 1. Store throws with status 404/409. Store collections: users (internal passwordHash extra, sanitize outputs), sessions, projects, annualGoals, plans, tasks, weeklyRecords, events, publications, reports, settings, scheduleRuns. No network I/O inside transaction.

server/auth.ts exports requireAuth(store):RequestHandler attaching req.user:User; requireManager middleware; safeUser strips secrets. server/domain.ts exports Domain(store) containing business logic used by router and tests. server/app.ts exports createApp({store?,dbPath?,enableScheduler?}={}): Express. Root agent implements server/reports.ts and server/report-routes.ts exporting createReportRouter(store):Router, mounted behind requireAuth at /api; server/scheduler.ts exports startScheduler(store):()=>void. API/domain agent integrates those imports only after files exist. Route bootstrap may import report methods only if needed; store.list('reports') manager only.

Frontend src/api.ts exports api<T>(path,options?) using /api prefix, credentials same-origin and error handling. App consumes bootstrap, displays create/review/publish weekly operations and reports, refreshes bootstrap after successful mutation. Root owns src/pages/Reports.tsx and server report modules; frontend agent owns remaining src and styling. Reports page exported default with props {data:Bootstrap,refresh:()=>Promise<void>,notify:(message:string)=>void}; use api and src/ui.tsx shared helpers if available.

## Team registration (2026-09-09)

- POST /api/auth/register {name,email,position?,password} -> 202 {message}. Requires an initialized active manager; creates inactive pending member, never a session. Email is case-insensitive and unique. Separate IP budget:20 attempts per15 minutes, including unsuccessful requests;429 returns Retry-After.
- POST /api/users/:id/registration-review {version,decision:'approve'|'reject',comment?} -> User. Manager only; reject requires comment. Only pending/rejected can be reviewed. Approval fixes role=member and active=true. Both transitions invalidate old credentials versions and audit atomically.
- User.registrationStatus is optional for compatibility with existing accounts. Access requires active=true plus absent/approved registrationStatus. Pending/rejected accounts are excluded from ordinary member directories and report snapshots. Manager bootstrap includes review comments. Normal account PATCH cannot activate or promote unapproved applications.
- All password creation/reset rules accept8–256 characters, without mandatory character classes. Registration form additionally checks confirmation equality.

## Friday whole-sheet submissions (2026-09-11)

Types in `shared/weekly-submissions.ts` define the response. W is the Monday of the cutoff cycle: `results` covers W, `plan` covers W+7. The default deadline is Friday 16:00 Asia/Shanghai; a versioned policy can use the last working day in Monday–Sunday at 16:00. Existing cycles and duties read their persisted deadline. Only server submission time strictly before the cutoff is on time. This receipt is independent of `WeeklyRecord.submitted` and task completion.

- GET /api/weekly-submissions?week=YYYY-MM-DD -> WeeklySubmissionView. Returns rule, server clock, cutoff, cycle and duty views. Members receive only their own duties, records, receipts and roster entry; managers receive the frozen roster. Each duty includes its version and complete sorted record ID/version manifest.
- POST /api/weekly-submissions/submit {dutyId,version,requestId,manifest,progressEventIds?,draftAction?:'include'|'retain',note?,reason?} -> immutable WeeklySubmission. Owner or manager; manager proxy requires reason. Every source row, including retained drafts, must match the preview manifest or the transaction returns 409. Where related progress exists, progressEventIds must also match the preview. Results require actual progress; plans require commitments; an empty sheet needs a note. Stable requestId retries return the existing receipt. Revisions preserve first valid submission and cutoff-missing facts. Plan submission enters applicable review; it does not approve its rows.
- PUT /api/weekly-submissions/rule {version,enabled} -> WeeklyRule. Manager only. Default starts next full week; pause/resume affects next week, preserving historical active windows.
- PUT /api/weekly-submissions/deadline-policy {version,mode:'friday'|'last_workday',calendarOverrides?:Record<YYYY-MM-DD,boolean>,calendarVersion?:number} -> WeeklyRule. Manager only. Supplied calendars require the current calendarVersion; stale rule/calendar versions return 409 atomically. Reuses collaboration settings, defaults Monday–Friday to working and weekends to rest. Saves a policy snapshot from the next complete week, preserving prior effective policies and rejecting an already-generated next cycle. It never rewrites existing cycles. The manager-only view.workCalendar provides the editable calendar and its version.
- POST /api/weekly-submissions/deadline-repair/preview {week} -> WeeklyDeadlineRepairPreview. Manager only; read-only, requires an existing cycle and never initializes rules or data. Calculates from the saved shared calendar in last_workday mode, returns old/new deadline, working-day snapshot, duty count, eligibility/reasons, unchanged flag and a ten-minute signed token. Only current-week cycles with reviewed rosters, both cutoffs still in the future, no submissions/missing/adjustments/reviews/report or period-review references, consistent duties and no change between duty/no-duty status are eligible.
- POST /api/weekly-submissions/deadline-repair {week,token,reason} -> WeeklyDeadlineRepairPreview. Manager only, nonblank reason required. Transactionally revalidates actor, operation epoch, token, calendar, cycle/duties and all protected facts. Atomically changes cycle and duty deadlines/snapshots, preserves roster/frozenAt and appends deadline_repair audits. Identical actor/week/reason/token retries have no duplicate writes; stale/expired or altered previews return 409. Runtime receipts are excluded from business JSON.
- POST /api/weekly-submissions/adjust {dutyId,version,action:'exempt'|'revoke_exemption'|'invalidate'|'restore',submissionId?,reason} -> WeeklyAdjustment. Manager only. Submission ID required for invalidate/restore. All changes append an audit fact rather than delete history.
- POST /api/weekly-submissions/roster {week,version,rosterIds,reason} -> WeeklyCycle. Manager confirms only a cycle marked needsReview. Normal rosters freeze eligible members as of Monday; later joins enter next cycle. Frozen roster changes use individual exemptions.

Reconciliation runs on startup, scheduler ticks and relevant API access. It catches up every enabled cutoff cycle idempotently. Unknown historical rosters wait for confirmation. Exemption affects current compliance counts, while missing-at-deadline evidence stays available. Reconciliation itself does not send external messages; the opt-in DingTalk worker below handles new, currently valid reminder events.

Under last_workday mode an all-rest cycle has deadlineAt:null, a frozen deadlinePolicy and no duties. It creates no missing facts/reminders and does not require the corresponding following-week plan review. A duty always has a concrete deadline. Reminders and management summaries use saved actual cutoffs; the delivery worker rejects obsolete queued slots. Repair snapshots use policyVersion:0; ordinary new cycles bind to their immutable effective rule policy. Storage format v4 blocks incompatible older applications; transfer v7 is selected when new policy/snapshot/repair-audit fields occur and still accepts v1–v6. Frozen reports remain unchanged; new report summaries carry their own deadline snapshot.

New report snapshots may contain optional `weeklySubmissions` summaries, frozen independently of delivery metrics. Legacy reports without this field remain valid. Business migration v2 includes weekly rules, cycles, duties, receipts, missing facts and adjustments with strict reference checks; v1 packages add no invented weekly facts. Member exports contain only personal facts, without departmental rules/rosters, and require matching destination cycles for restore. Safe goal projections cannot be restored as complete goals; use the administrator’s complete package.

Migration requires canonical ISO UTC milliseconds for new weekly timestamps (including nested report/audit facts), preventing timezone-string comparisons from changing compliance. Restore preview exposes `notices` and per-collection `replace` counts when accepting a source rule over an untouched, unused server default; configured or used rules remain conflicts. Replacement preserves source metadata, is audited, and is revalidated transactionally.

## Weekly plan review and deletion (2026-09-20)

- POST `/api/weekly-submissions/review` `{dutyId,version,submissionId,decision:'approved'|'returned',reason?,requestId}` -> immutable `WeeklyPlanReview`. Manager only; return requires nonblank reason. `version` is the duty version shown in the preview. The transaction requires the latest valid, non-exempt plan receipt and rechecks the complete planning manifest; changed/deleted/added rows, stale versions or an already reviewed receipt return 409. One decision per submission. An identical reviewer/requestId retry returns its existing result; reuse for different content returns 409.
- Duty views add `planReviewRequired`, `planReviewStatus:'not_required'|'unsubmitted'|'pending'|'approved'|'returned'|'changed'`, `latestPlanReview` and `planReviews`. Existing compliance `status`, first submission time, deadline and missing facts remain separate. A returned plan is revised by a new whole-sheet submission; the original receipt and decision remain readable.
- New plan receipts add `planManifest?:{id,fingerprint,submitted}[]`, including retained drafts, `planTaskSnapshots?:{id,title,dueDate,description}[]` and `planGoalSnapshots?:{id,month,title}[]`. Context is frozen at submission. The command's complete ID/version `manifest` protects submission concurrency; the plan manifest protects review against business-content changes while permitting later execution progress.
- `WeeklyRecord.planApproval?:{required:true,approvedSubmissionId:string|null,approvedFingerprint:string|null,suspended?:true}` records the row's effective approval. The fingerprint is `JSON.stringify([taskId,ownerId,weekStart,monthlyPlanId,commitment])`; execution status, progress, evidence, storage version and approval metadata are excluded. A changed row stops contributing to effective weekly counts until approved again; unchanged rows keep their own prior approval. A required row is effective only while active and submitted, with a matching approved fingerprint or a rule-derived suspension. Missing metadata remains compatible with history; normal writes and restore normalization prevent applicable new member work from bypassing review.
- A manager's explicit formal assignment is confirmed; an assigned draft and a proxy recording are not approvals. A member's change to an assigned commitment requires review in applicable cycles. Ordinary client writes cannot set approval/deletion metadata. Monthly publication, ownership/participation, temporary-work and month-overlap validation continue independently.

`WeeklyRule.planReviewEffectiveWeek` is the persisted first **submission cycle**, not the content week: cycle W reviews content W+7. First initialization chooses the next full cycle; restarts retain it and do not create historical approval facts. Review applies only when W also belongs to a rule window. Pausing/resuming the submission rule changes windows from the next full cycle and synchronizes affected future rows. Closed cycles retain `suspended:true` and their old approval fields; only newly reopened cycles resume the requirement. Already elapsed paused cycles stay exempt after later resumption. Original duties, reviews and cutoff facts are retained. Explicitly invalidating an approved receipt clears current row approvals referencing it; restoring that receipt can reinstate its approval only for unchanged, active, submitted rows without another current approval. Frozen history is unchanged in both cases.

Weekly review metadata and a default-empty `weeklyPlanReviews` collection remain compatible with older packets. Export selects the smallest required supported format: v2 for base/weekly facts, v3 for collaboration facts, v4 for report-agent facts, v5 for delivery/decision or expanded coordination facts, v6 for commitment/evidence/period-review facts, and v7 for calendar deadline policies/snapshots/repair audits; v1–v7 inputs remain accepted under their strict schemas. Reviews contain duty/owner/cycle/submission IDs, decision, reviewer, review time, reason and requestId. Strict schemas cover nested snapshots; task/goal contexts and approval references travel with their dependencies. Administrator exports include tombstones even though bootstrap omits them. Restore checks Task/week uniqueness only among non-deleted rows and remaps owner IDs inside historical fingerprint tuples as well as reviewer/deleter IDs.

Restore rejects broken approved snapshots or reviews, duplicate decisions and a live approval pointing to an invalidated receipt; frozen reports/audits may retain the original historical approval. A live `suspended:true` must correspond to a post-activation cycle outside the current rule windows or an all-rest cycle under its saved last_workday policy; historical snapshots are not reinterpreted against today's window or calendar. Applicable active member rows in an old packet without approval metadata normalize to pending during the shared preview/restore pass, except already issued manager assignments. Assigned drafts still normalize to pending. If an incoming legacy rule lacks the optional review boundary, it inherits a persisted target boundary with a preview notice. This does not rewrite nested history or create approvals. Restore produces no new inbox notifications or external deliveries. Member packets remain scoped to personal facts and require destination policy/cycle dependencies; administrator export is the complete migration path.

## DingTalk access and notifications (2026-09-18)

`shared/notifications.ts` defines inbox, targets, delivery and settings response types. All writes retain the existing JSON/same-origin guard. No incoming callback endpoint is required for this phase.

- GET `/api/auth/dingtalk/config` returns only configured state, corpId and public clientId. POST `/api/auth/dingtalk/exchange` `{code}` verifies internal-app identity and returns `{authenticated:true,user}` plus a normal session, or `{authenticated:false,bindingRequired:true}` with a five-minute HttpOnly browser proof. It never creates users. Exchange is rate-limited and codes are single-use.
- GET `/api/dingtalk/binding` requires ordinary authentication and returns `{bound,corpId?,boundAt?,pending?:{corpId,userid,expiresAt}}`. POST `/bind` with `{}` explicitly consumes the same browser proof for the logged-in, active approved account. POST `/unbind` with `{}` removes binding and revokes existing sessions. Provider identity and local account are each unique; names are never matched automatically.
- GET `/api/notifications?filter=all|unread|pending&cursor=...&limit=50` filters the recipient's complete projected inbox before cursor pagination. The limit defaults to 200 and is capped at 200 per page. Returns `items`, `unreadCount`, `pendingCount`, `totalCount`, `filteredCount`, and `nextCursor`; counts cover the complete authorized inbox, including records older than the first 200. Cursors are bound to recipient/filter and sort by createdAt plus ID. GET `/:id` is read-only; POST `/:id/open` and `/:id/acknowledge` with `{}` separately record local opening and owner acknowledgement. POST `/open-all` affects only the current recipient's viewed state, never acknowledgement. A notification ID grants no business access. Lost permissions redact details; expired assignment versions cannot be acknowledged.
- GET `/api/notification-status?type=plan|task|weeklyRecord&id=...` is manager-only and returns `{items:NotificationView[]}` for the source detail panel. POST `/api/notifications/:id/remind` is manager-only, requires a still-pending acknowledgement, and permits one reminder per recipient/target/Shanghai day without creating a new acknowledgement version.
- GET and PUT `/api/notification-settings` are manager-only. PUT takes `{version,externalEnabled,pilotUserIds,sendStartHour,sendEndHour}`. Enabling requires configured credentials, HTTPS, server opt-in, a deployment ID and a nonempty pilot. POST `/api/notification-deliveries/:id/retry` retries only explicitly failed deliveries with unchanged activation and binding; unknown sends cannot be blindly replayed. Settings, retries, bindings and manual reminders have dedicated audit records outside business exports.
- POST `/api/weekly-assignments` `{requestId,taskId? OR task:{...taskInput},record:{...weeklyInput}}` creates the task if needed, weekly row and notification atomically. Actor-scoped requestId is 16–100 letters/digits/underscore/hyphen. An identical retry returns current entities; changed content with the same key is a 409. A failed row creation leaves no orphan task or notification. Task creation kind governs the new task+row pair.

Live actions enqueue inbox and outbox in the same SQLite transaction. Draft/unpublished, self-created, proxy and imported work remain silent. Repeated publication of unchanged weekly work does not reset owner acknowledgement. Provider acceptance, provider-reported success, system opening, acknowledgement and formal business receipts are distinct. Safe retries use 1/5/15/60-minute delays with five attempts; missing/uncertain async send receipts become unknown, and known receipts are polled up to 24 hours. Changed assignments coalesce pending external deliveries for five minutes; ordinary backlog expires after seven days. Friday reminders use formal duty views and expire at cutoff, with only the latest current slot eligible; summaries expire at Friday midnight.

Identity, binding audit, notification/obligation/work-version state, activation settings, delivery records, reminder quotas and atomic-request receipts are excluded from business JSON transfer. Whole-database backups retain them. Before cloning/restoring, disable external send and use a new deployment ID as described in `docs/dingtalk-public-server.md`.

## Problem feedback (2026-09-21)

`shared/feedback.ts` defines all request and response types. Routes require an active approved session; writes use the existing same-origin JSON guard. Members can create and read only their own feedback. Managers can read and process all feedback; confirmation of a working fix belongs only to that feedback's reporter, including when the reporter is a manager. Feedback is separate from plans, task completion, KPI and import batches.

- GET `/api/feedback/meta` -> `{managers:[{id,name}],defaultAssigneeId}`. The default receiver is the first available manager. Only active, approved managers are assignable.
- GET `/api/feedback?scope=mine|all&status=...&assigneeId=...&cursor=...&limit=...` -> `{items,counts:{all,new,in_progress,verification,closed},nextCursor}`. `scope` defaults to `mine`; `all` and receiver filtering are manager-only. `limit` defaults to 30 and accepts 1–100. Receiver/permission filters precede counting and pagination; status counts cover that complete scope, independent of the selected status tab. Opaque cursors order by descending update time and ID. Items include reporter/receiver names and screenshot counts, never screenshot bytes. An unavailable receiver is marked in `assigneeName` and `assigneeAvailable:false`; reads do not silently reassign responsibility.
- GET `/api/feedback/:id` -> `{feedback,events,attachments,allowedActions}`. The timeline and attachment list contain metadata only. Unauthorized or missing feedback returns 404. Members receive only `duplicateLinked`, without the primary issue ID or access to that other reporter's feedback.
- POST `/api/feedback` `{requestId,description,kind?,impact?,context?,attachments?}` -> 201 with the same detail response. Description is required, up to 10,000 characters. `kind` is `bug` (default), `usability` or `suggestion`; `impact` is `normal` (default) or `blocking`. Context only retains `path`, `appVersion`, `userAgent`, `viewport` and `errorRequestId`; the path retains known page names and date filters while stripping credentials, other query values and fragments.
- POST `/api/feedback/:id/actions` `{requestId,version,action,...}` -> updated detail. See the action rules below. Every accepted write, including its events, attachments, responsibility changes, command receipt and in-app notifications, commits in one synchronous SQLite transaction.
- GET `/api/feedback/:id/attachments/:attachmentId` returns the authorized image bytes. Every request checks the current session and feedback scope; another feedback ID cannot be used to fetch the attachment. Responses use the validated image MIME, `X-Content-Type-Options:nosniff`, a sandbox policy and `Cache-Control:private,no-store`.

The four statuses are `new` (待受理), `in_progress` (处理中), `verification` (待验证) and `closed` (已关闭). Additional waiting information lives in `waiting:{kind:'request_info'|'defer',reason,reviewAt}` rather than inventing another completion state. Current allowed actions are returned by the detail endpoint:

- `comment`: reporter or manager; requires `text` or at least one screenshot. A reporter's supplement clears an outstanding `request_info` flag. Comments may be appended to closed feedback without reopening it.
- `assign`: manager; requires an available manager's `assigneeId`. `start` moves a new/in-progress issue into active processing and clears its waiting flag.
- `request_info`: manager; requires `reason` describing what to supplement. `defer` requires both `reason` and a future `reviewAt`; the timestamp is stored in canonical UTC. Both remain `in_progress`.
- `ready`: manager; requires nonblank `resolution`, `releaseVersion` and explicit `released:true`, meaning the fix is actually available for verification. It enters `verification` and does not close the issue. A release already rejected in this feedback cannot be announced as a new fix using the same version.
- `confirm`: reporter only, from `verification`; records `closure.kind:'confirmed'`. `reopen` is available to the reporter or a manager from verification/closed, requires `reason`, retains the timeline and records which release failed verification.
- `close`: manager; requires `reason` and records `closure.kind:'manager'`. This is displayed separately from reporter-confirmed resolution.
- `duplicate`: manager; requires `duplicateOfId` and `reason`; rejects self-links, cycles, missing primaries and redundant links. It keeps the local issue open, forwarding only generic result text and an available release version through open descendants. Primary descriptions, comments, screenshots and reporter identities do not propagate. Linking a chain to an already released/confirmed primary also updates its open descendants. Each reporter confirms independently; primary confirmation and repeated announcements of the same rejected version cannot override a linked reporter's failure. Independently closed feedback remains closed.

When a successful write finds a receiver who is disabled, unapproved, missing or no longer a manager, the transaction selects the first available manager and records a system assignment event plus in-app notifications. This also applies when reopening closed feedback and when propagating a result to an open linked issue. With no available manager, the operation fails with 409 without recording a misleading acceptance; the client retains its input for recovery.

Each command needs an actor-scoped `requestId` of 1–160 letters, digits or `_.:-`. The content fingerprint includes the operation and submitted version. An identical retry returns the currently authorized detail before performing stale-version checks, without repeating events or notifications. Different content using the same ID, or a fresh command with an outdated version, returns 409. After a version conflict, refresh, review the retained input and submit with the current version and a new request ID. API error bodies include the server-generated `requestId`, matching `X-Request-Id`; it is distinct from the client command ID.

Attachments use `{name,mimeType,dataBase64}`: PNG, JPEG or static WebP only, at most three per operation and at most 2 MiB decoded bytes each. Feedback creation/actions have a 9 MiB JSON limit; other API body limits are unchanged. The server checks canonical base64, declared type, container structure, dimensions, PNG checksums/decompressed rows, JPEG tables/nonempty scans and WebP payload/partition bounds. This is not a complete JPEG/WebP pixel decoder; the browser also attempts actual image decoding before adding a screenshot. Unsupported, damaged, oversized or excessive images reject the whole operation. Binaries are stored only in the protected `feedbackAttachments` collection.

Feedback notifications use a `feedback` target and remain strictly in-app: enqueueing creates no external delivery or work-acknowledgement obligation, and the worker refuses feedback even if an otherwise eligible external delivery was erroneously queued. Reading a message never confirms a fix. The `feedback`, `feedbackEvents`, `feedbackAttachments` and `feedbackCommands` collections are retained by whole-database backup and excluded from business JSON transfer. Reporter, receiver, attachment and historical operation references participate in account deletion blockers. This API documentation describes the local implementation, not a production deployment.


## Unified tasks, delivery, authorization and responsibility (2026-09-22, phase two)

Contracts: `shared/task-view.ts`, `shared/work-progress.ts`, `shared/my-actions.ts`, `shared/object-access.ts`, `shared/deliveries.ts`, and `shared/support.ts`. The following paths are relative to `/api`. These are implemented local contracts; deployment and browser acceptance are tracked separately in [phase-two validation](validation-2026-09-22-delivery-management.md).

### Unified task reads and editable projections

| Method and path | Result and access |
| --- | --- |
| GET `/tasks/:id/view?section=&weeklyRecordId=` | `TaskView`: authorized task/owner, weekly records, minimal or historical monthly goal reference, `progress`, `allowedActions`, `readOnlyReason`, paginated `taskHistory`, and compatibility collaboration fields. Owner/manager retain their business scope. Observers use the same safe object projector as authorized work. |
| GET `/tasks/:id/history?cursor=&limit=` | `{items:TaskHistoryItem[],nextCursor}`; default 30, range 1–100. Safe timeline rows include ID/kind/time/title/detail, never raw audit before/after objects. Cursors bind actor, task, access scope and operation epoch. |
| GET `/tasks/:id/editable` | `{version,values,operationEpoch}`; current owner/manager only, whitelisted editable task fields. A cancelled task returns 409 `TASK_CANCELLED`. |
| GET `/weekly-records/:id/editable` | Same envelope for editable weekly fields, with optional `relatedTask:{id,version,status,completionNote}` for the currently authorized parent. Both weekly ownership and parent task access are checked. Deleted rows return 409 `WEEKLY_RECORD_DELETED`; cancelled parent tasks return `TASK_CANCELLED`. |

`section` must be one of `overview`, `weekly`, `deliveries`, `support`, `followups`, or `history`. A supplied weekly ID must belong to this task and be readable by this actor; foreign/unreadable IDs return 404. The selected week does not change the task identity or overall status. Authorized deleted weekly records and cancelled task history remain readable, without editable forms. The older `/collaboration/tasks/:id` route remains available for compatible business clients.

`allowedActions` describes currently possible base actions (`edit_task`, `update_weekly`, `submit_delivery`, `manage_support`, `manage_grants`); it is a UI affordance, not a substitute for command authorization. Base task editing, delivery, progress facts and support responsibility do not depend on the collaboration feature switch. Optional followups, escalation and outbound channels retain their existing feature gates.

`WorkProgress` keeps separate values:

- `overallProgress: {text,changedAt:null|string,evidenceRef:null|string}|null` represents task-level prose and its proven field-change time.
- `latestExecution: {text,sourceType,sourceId,weekStart?,occurredAt,recordedAt,actorId,proxy,evidenceQuality}|null` represents the latest meaningful actual execution fact. Source type is task/weeklyRecord/progress and quality is recorded/audit_reconstructed/unknown. Times may be null.
- `historicalExecution` retains text without a defensible execution timestamp. The UI labels unknown historical update times; it does not rank them using Task.updatedAt or the read time.

Future-week promises, deleted weekly records, title-only changes and no-change responses do not advance the meaningful execution clock. New weekly outcomes do not overwrite overall prose. Member projection filters other owners' weekly events and unauthorized earlier task history before choosing the latest event. Work-register snapshots and CSV expose separate overall/latest columns while preserving completion/blocker/history supplements.

### Observer and object authorization

`observer` is a read-only role even when the account formerly owned business objects. The server re-reads the current account for service calls and applies a deny-by-default HTTP guard: observer writes return 403, and unsupported legacy reads return 404. Allowed read routes are auth identity, bootstrap, my-actions, authorized-work, unified task view/history, and individually authorized scoped-report reads/exports. Original reports/report-agent assets, broad task/weekly endpoints, feedback attachments, integration tokens and business migration are not opened by a task grant.

Observer bootstrap returns the observer identity, `authorizedWork`, `scopeVersion`/`accessScopeVersion`, the operation epoch, and empty legacy business/dictionary arrays. It does not reuse the member workspace projection. No relation transitively grants another object: task grants contain only minimal monthly/project references; project summaries count separately authorized tasks only.

| Method and path | Input / result |
| --- | --- |
| GET `/object-grants` | Manager only; `{items:ObjectGrant[]}`, including revoked grants for versioned regrant. |
| POST `/object-grants` | Manager; `{requestId,subjectId,objectType,objectId,objectVersion,capabilities,historyPolicy?,reason,expiresAt?,version?}` → `ObjectGrant` (201). First version accepts observer recipients only. |
| POST `/object-grants/:id/revoke` | Manager; `{requestId,version,reason}` → current `ObjectGrant`. |
| GET `/authorized-work` | `{items:AuthorizedWorkItem[],scopeVersion}` for current effective grants. |
| GET `/authorized-work/:id` | `:id` is a grant ID; returns `{objectType,objectId,object,capabilities,scopeVersion}` after current authorization. |
| POST `/scoped-reports` | Manager; `{requestId,subjectId,title,taskIds,includeHistory?}` → immutable `ScopedReport` (201). Requires 1–100 unique tasks already granted to the observer. |
| GET `/scoped-reports` | Manager only; `{items:ScopedReport[]}` for managing previously generated summaries. |
| GET `/scoped-reports/:id` | Current authorized report projection. A report grant alone does not grant any source fact. |
| GET `/scoped-reports/:id/export` | Independently checks `export_summary`, including source permissions; returns a text/plain attachment with title, narrative and hash. |

Object types are `task`, `project_summary`, and `scoped_report`. Capabilities are `read`, `read_evidence`, and `export_summary`; every grant requires `read`. A grant records subject/object/version, granting manager/time, reason, expiry, revocation, history policy and exact excluded historical fact IDs. Updating or regranting the same subject/type/object updates the existing grant with its current `version`; it never stacks duplicate grants. `objectVersion` must match the selected object. Expiry is a future canonical UTC ISO timestamp or null.

`historyPolicy` defaults to `current_onward`: current object state remains visible and may contain earlier facts, while preexisting weekly/submission/history facts are excluded using the grant's saved boundary, including same-timestamp facts. `all_history` includes authorized historical facts but still excludes private unsubmitted drafts and private causes. An uninterrupted grant whose policy stays the same retains its boundary when capabilities/expiry change. Revocation, expiry or policy change establishes a new boundary on regrant. Observers see safe current delivery status counts (`authorizedDeliverySummary`) separately from history-filtered submission prose (`authorizedDeliveries`).

Scoped-report prose is regenerated from permitted source facts; it is never a truncated snapshot paired with an original whole-department narrative. Generation does not automatically grant the new report. A separate report grant and current grants for every source fact are required on each read/export. The manifest records object/fact IDs, versions and historical timing. Revoked, missing, mismatched or unknown historical source facts deny the whole frozen report; regranting current_onward does not restore an old all_history report. The frozen narrative is not rewritten during a read. Evidence is separate from prose and requires `read_evidence` at both report and source scopes. External links remain subject to the external system's access controls.

Account disablement, expiry or revocation is effective on the next authorized read/action/export/send. Clients invalidate cached objects when their access scope changes and clear forbidden/not-found reads; links are not credentials. Already delivered or downloaded copies cannot be recalled.

### Personal delivery versions and decisions

The stable `DeliverySeries` holds task/title/current reviewer/head submission/status/version. `TaskDelivery` is an immutable revision containing actual owner and submitter, proxy reason, task version, outcome/evidence, acceptance criteria, reviewer, deadline and approved-deadline-basis snapshots. `DeliveryDecision` is immutable; corrections point to the previous effective decision with `supersedesDecisionId`.

| Method and path | Input / result |
| --- | --- |
| GET `/tasks/:id/deliveries` | Owner/manager; `{items:DeliverySeriesView[],eligibleReviewers,canSubmit}`. Each row includes current submission/decisions, current reviewer availability, allowed actions, first submitted time, accepted-version submitted time and accepted time. Observers use unified safe task projection instead of this business endpoint. |
| GET `/delivery-series/:id/history?cursor=&limit=` | Owner/manager; `{items:DeliverySubmissionView[],nextCursor}`. Default 20, range 1–100; cursor represents an earlier revision boundary within the authorized series. |
| POST `/tasks/:id/deliveries` | `{requestId,taskVersion,previousRevision,actualOutcome,evidenceRefs,acceptanceCriteria,reviewerId,seriesId?,seriesVersion?,previousSubmissionId?,title?,newSeries?,proxyReason?,replaceAccepted?,markTaskDone?,completionNote?}` → `DeliveryMutationResult` (201). |
| POST `/deliveries/:id/decisions` | `{requestId,seriesVersion,action:'review'|'withdraw'|'correct',conclusion?:'accepted'|'returned',note,supersedesDecisionId?}` → `DeliveryMutationResult`. |
| POST `/delivery-series/:id/reassign` | Manager; `{requestId,seriesVersion,reviewerId,reason}` → `DeliveryMutationResult`; records the assignment change without rewriting original submission snapshots. |

`DeliveryMutationResult` is `{series,delivery,decision?,task}`. First submission uses previousRevision 0 and no previous submission. Continuation requires the current series version, prior revision and exact prior submission ID. A task's first default series is created atomically with v1; a different additional deliverable requires `newSeries:true`. Pending submissions cannot be edited or superseded until withdrawn. Accepted replacement requires `replaceAccepted:true`; prior acceptance remains in history and the new head returns to pending_review.

States are `pending_review`, `accepted`, `returned`, and `withdrawn`. Review applies only to the current pending head. Accepted/returned review and correction require the current active designated manager, who cannot be the submission owner. Other managers must first explicitly reassign with a reason. Withdraw is available only for the current pending head to its submitter or a manager. All decisions require a note; correct references only the selected revision's current effective decision and cannot convert a decided revision to withdrawn. Correcting an older revision changes its effective decision without changing a newer head's status. Series CAS, not the immutable submission's version, resolves review/withdraw/correction races.

Outcome and acceptance criteria are nonblank. Evidence is up to 30 links or text items; URL schemes are limited to HTTP(S). If any active nonowner manager exists, one must be selected; `reviewerId:null` is accepted only when the server confirms none exists. Such a submission retains its real time and remains pending assignment. A disabled reviewer likewise requires explicit reassignment. Manager proxy submission preserves the actual owner and requires a reason.

Submission/acceptance/return never silently changes Task.status, weekly submission or monthly acceptance. Explicit `markTaskDone:true` uses the same transaction and first-batch completion validation. First submission time, current accepted revision's submission time, acceptance time and reviewer waiting time are separate. Delivery timeliness uses the accepted revision's submitted time against its frozen deadline; late reviewer handling is not member lateness. Without acceptance, display submission timeliness and quality pending, not an accepted on-time rate. Legacy completed tasks receive no invented submission/acceptance.

### Assigned support and decision responsibility

Blocker episodes retain technical `resolvedAt/resolvedBy` independently of `managementClosedAt`. New coordination fields include `coordinatorId`, `responseDueAt`, `coordinationState` and `responseNote`. States are unassigned/awaiting_response/in_progress/responded/management_closed. Legacy enrollment uses `openedAtKnown:false` when the actual occurrence time cannot be established.

| Method and path | Input / result |
| --- | --- |
| GET `/tasks/:id/support` | Owner/manager `TaskSupportView`: blockers, decisions, eligible coordinators/decision owners and create/enroll flags. |
| GET `/blockers/:id` | Owner/manager or exact current coordinator; `BlockerView` contains episode, safe actions, minimal task identity, `minimalContext`, coordinator availability and allowed actions. |
| POST `/tasks/:id/blockers` | Owner/manager; `{requestId,taskVersion}` explicitly enrolls a current legacy task blocker; returns its episode. |
| POST `/blockers/:id/assign` | Manager; `{requestId,version,coordinatorId,responseDueAt,reason}` → `{episode,action}`. |
| POST `/blockers/:id/handle` | `{requestId,version,action:'record'|'respond'|'defer'|'close',note,reviewAt?}` → `{episode,action}`; legacy route delegates to this base service. |
| GET `/decision-requests/:id` | Authorized business decision projection, including allowed actions. |
| POST `/decision-requests` | Manager; `{requestId,taskId,taskVersion,question,options,decisionOwnerId,responseDueAt,blockerEpisodeId?,reason?}` → `DecisionRequest` (201). |
| POST `/decision-requests/:id/decide` | Current designated active manager; `{requestId,version,result,reason?}` → `DecisionRequest`. |
| POST `/decision-requests/:id/reassign` | Manager; `{requestId,version,decisionOwnerId,reason,responseDueAt?}` → `DecisionRequest`. |
| POST `/decision-requests/:id/cancel` | Manager; `{requestId,version,reason}` → `DecisionRequest`. |
| POST `/decision-requests/:id/reopen` | Manager; `{requestId,version,reason,responseDueAt,decisionOwnerId?}` → new open business generation on the same request. |

Only the current assigned coordinator may respond as coordinator; record/respond do not grant access to the full task, earlier management discussion, previous assignee replies or other weekly drafts. Reassignment revokes the former coordinator's minimal read, write and inbox access. Active managers may record arrangements, accept a deferral or close management support. A coordinator's proposed review time is recorded on the action; it does not itself establish a scheduled manager-approved revisit. Management closure does not resolve the technical blocker or mark the task done. Decision responsibility requires an explicitly selected effective manager; free-text names in `decisionNeeded` are not inferred as accounts. Cancel/reopen preserve reason/audit and increment business generation on reopening.

`GET /collaboration/resolve/blocker/:id` returns `{taskId,blockerId,minimalContext}` after current blocker authorization; decisionRequest resolution returns its task/request context. In-app notifications can target `blocker` or `decisionRequest`; unrelated coordinators open the minimal blocker panel. Base responsibility and in-app obligations persist with collaboration disabled; optional external delivery remains gated.

### My actions and command recovery

GET `/my-actions?kind=&cursor=&limit=` → `{items,counts,totalCount,filteredCount,nextCursor,asOf}`. Default limit 30, range 1–100. Kinds: monthly_review, monthly_acceptance, weekly_review, delivery_review, deadline_review, followup_response, support, decision, assignment, revisit. Each item contains a stable business-generation key, source ID/version, assignees, required action, optional task/due/blocking context, `sharedQueue` and an action target.

Authorization precedes aggregation/filtering/pagination. Counts cover the full authorized set; page size does not affect counts. Sort is overdue first, then due time, creation time and stable key. Cursor scope binds account, filter, permission scope and operation epoch. Missing/changed anchors require a first-page refresh. Unassigned review is explicitly a shared management queue; the original business command's CAS permits only one effective decision. Deactivated assignees and gated legacy operations enter the manager assignment/recovery queue. Approved future revisits remain in revisit until due; a formal response consumes that revisit. Cancelled task sources leave current actions but retain history. Message opened/read/delivered state does not complete actions, and there is no generic complete-action mutation.

New delivery/support and grant/report commands use actor-scoped request IDs of 8–100 letters, digits, `_` or `-`. Reuse the same ID and unchanged payload after a network-unknown outcome. Mutation, immutable evidence, audit, notification intent and receipt are committed together where applicable. A matching receipt is read before stale submitted-version checks, but current actor/object authorization is rechecked: downgrade, disablement, revoked responsibility or lost parent scope cannot be bypassed with a cached receipt. Replay returns current authorized task/series/episode/request or grant state; immutable original submission/decision/action evidence remains unchanged. It does not repeat side effects or return a obsolete cached status as current. Changed content under the same request ID returns `IDEMPOTENCY_MISMATCH`. Deliberate reviewed new edits use a new command ID.

| Error code | Client behavior |
| --- | --- |
| 409 `VERSION_CONFLICT` | Keep local input; obtain a current authorized editable projection where available and compare B/S/L. Workflow actions reload current state without blind mutation retry. |
| 409 `TASK_CANCELLED`, `WEEKLY_RECORD_DELETED` | Read-only historical object. Preserve/copy local input; no merge-to-write. |
| 409 `IDEMPOTENCY_MISMATCH` | Explain command identity reuse; do not treat it as a field conflict. |
| 409 `ACCESS_SCOPE_CHANGED`, `ACTION_LIST_CHANGED` | Reset pagination and reload the authorized list; not a form merge. |
| 403 `READ_ONLY_OBSERVER`, 403/404 `ACCESS_REVOKED` | Remove inaccessible server content from client caches; never expose a replacement server snapshot in the error. |
| 401 or other permission loss | Retain account-scoped local input for recovery, without reading privileged content. |

Session drafts remain local with seven-day retention. Schema 3 stores user/entity/form identity, operation epoch, baseVersion/baseValues, values, savedAt and formSchemaVersion; a per-form discovery index plus exact identity matching finds older versions. Legacy schema 2 has no invented baseline and requires a two-column choice. A three-way comparison is opened for 409 VERSION_CONFLICT, with unchanged-local → server, unchanged-server → local, equal → identical, and divergent changes → explicit choice. Status/completion, blocker fields and personnel sets are grouped. Merge confirmation only prepares a fresh versioned submission; it never force-overwrites or automatically saves. Another conflict repeats the comparison. Storage/quota failure and unknown network outcomes retain current input and do not claim a saved backup. A completed write followed by a failed read retries reads only.

Weekly completion also groups the related task status/completion note with the intent to complete the task. `taskVersion` advances only after explicit comparison confirmation; choosing the server side clears that local completion intent. If the authorized reread reports lost access, cancellation, or deletion, the form preserves input for copying and disables further mutation instead of preparing a retry.

### Business transfer v5

`formatVersion:5` adds `deliverySeries`, `taskDeliveries`, `deliveryDecisions`, `decisionRequests` and the expanded blocker coordination fields/actions to strict business schemas. Selected task exports include their dependency closure: series/head/prior revisions, effective and superseded decisions, approved deadline bases, linked blockers/decisions and responsible users. User mappings apply to owner, submitter, reviewer snapshots/current reviewer, deciding/requesting users, coordinator/technical resolver and audit references. All new roles remain subject to current authorization; an old manager object cannot export/restore after downgrade to observer.

Restore checks head/series/task consistency, contiguous unique revisions, exact supersedes chains, one effective terminal decision per decided revision, status/decision consistency, no cycles, nonself review after mapping, valid approved deadline bases, decision-to-blocker task ownership, required evidence/reasons and decision result/status consistency. Invalid references or chains reject preview/restore without partial writes. Historical v1–v4 packets remain readable with empty new delivery collections; they cannot smuggle nonempty delivery facts under an older format number. Actual restore writes rotate the operation epoch in the same transaction, emit no external sends, and preserve the existing no-op/rollback behavior.

`objectGrants`, `scopedReports`, `objectAccessCommands`, collaboration/delivery command receipts and other runtime sharing/subscription configuration are excluded from ordinary business migration. Whole-database backups still retain them and must follow operation-context reset procedures after restoration. New delivery/support/decision/grant/report account references block hard account deletion; deactivation preserves historical attribution.

## Third batch: cross-period workflow and historical review

All paths below are under `/api` and require a currently authorized account. Cross-period commands and historical review creation/finalization require an active manager. Observer grants never imply access to department review data. A task PATCH changing title, description, requested outcome or deadline requires `reason` or a valid `proxyReason`; normal progress-only updates remain unchanged. The internal silent-import context does not fabricate an initial live commitment.

### Cross-period workflow

| Method / path | Contract |
| --- | --- |
| POST `/carry-workflows/preview` | Read only: `sourcePlanId,targetMonth`; source outcomes, task/weekly manifests and existing carry targets. |
| POST `/carry-workflows` | `requestId,sourcePlanId,sourceVersion,targetMonth,remainingWork,reason`; choose `targetPlanId`, or `dueDate` for a new target and explicit `split:true` when applicable. Creates draft and workflow atomically. |
| GET `/carry-workflows?sourcePlanId=` | Current-epoch resumable/completed/cancelled workflow views. |
| GET `/carry-workflows/:id` | Source, target, latest tasks/weekly records, source changes and allowed continuation. Old-epoch workflows return 409. |
| PATCH `/carry-workflows/:id` | `requestId,workflowVersion,selectedTaskIds,targetWeek,commitments`; save the selection only. |
| POST `/carry-workflows/:id/preview-apply` | Pure read with the selected tasks, week and per-task commitments; returns complete manifest/fingerprint. |
| POST `/carry-workflows/:id/apply` | `requestId,workflowVersion,manifest,fingerprint,selectedTaskIds,targetWeek,commitments,reason`; transactionally revalidate and relink/schedule the original task IDs. |
| POST `/carry-workflows/:id/cancel` | `requestId,workflowVersion,reason`; ends orchestration, retaining created targets and business facts. |

Mutations use `X-Operation-Epoch`. Replays bind actor, epoch, request ID and canonical content and recheck current authorization. Approved-but-unpublished targets block apply. The final manifest also binds source/task set, all relevant weekly records, submission audits and frozen whole-week receipts; new/deleted/version-changed facts invalidate the preview. A target-week effective record is reused without overwriting its commitment. A conflicting record rejects the entire batch. Formerly submitted but withdrawn weekly records remain historical. Restored old-epoch workflows remain in whole backups, but users must start from the current source/target facts.

### Historical review

| Method / path | Contract |
| --- | --- |
| POST `/period-reviews/preview` | `period,cutoffAt,laterEvidenceThrough?,previousSnapshotId?`; returns facts, coverage, unknowns, weekly compliance, manifest, fingerprint and operation epoch. |
| POST `/period-reviews` | Same scope plus `requestId,operationEpoch,sourceManifest,fingerprint`; recompute in a transaction and freeze a draft. |
| GET `/period-reviews` | `{items}` metadata, filtered by current access. |
| GET `/period-reviews/:id` | Frozen snapshot; members receive only finalized cutoff-owner facts and their weekly duties. Department sources and comparison payloads are removed from the member projection. |
| GET `/period-reviews/:id/display-references` | Freshly authorized names for visible identities and, for managers, related plans/projects. Display labels do not modify frozen snapshot bytes or hashes. |
| POST `/period-reviews/:id/finalize` | `requestId,operationEpoch,version,contentHash`; immutable frozen content with finalization metadata. |
| POST `/period-reviews/evidence` | `requestId,operationEpoch,taskId,ownerId?,claimedAt,statement,evidence[],reason`; record separate historical supporting evidence. |
| GET `/period-reviews/:id/export` | Text export with the same period, cutoff, generation time, rule version, later-evidence boundary and authorized projection. |

Post-period evidence is an explicit revision referencing a finalized predecessor. A predecessor cannot acquire two children. Original finalized content is never overwritten. Formal delivery receipt time, applicable commitment, review waiting and weekly whole-report compliance remain separate. Evidence gaps remain unknown. Supplementary evidence never becomes a formal submission receipt.

### Bounded workspace reads

`GET /workspace` returns only identity, capabilities, current access scope version, operation epoch, necessary configuration and lightweight counts. It does not return the historical dictionaries, task collections or report bodies. Observer shell access is permitted; all `/workspace/*` business queries are denied to observers.

`GET /workspace/tasks|weekly-records|plans|history|progress|reports|candidates` returns `{items,total,nextCursor,revision,accessScopeVersion}`. Default limit 50, maximum 100. Accepted filters are explicitly whitelisted (`ownerId,status,month,weekStart,taskId,q,includeCancelled,scope,kind,type,period,role,cursor,limit`), with parameterized SQL. History/progress require a task. Candidates use `kind=user|project|plan`; user candidates may use `role=manager|member`. Invalid or changed query/identity/scope/data cursors return 409 `WORKSPACE_CURSOR_STALE` and require a first-page read.

Lists and totals use one transaction and the same authorization predicate. Open tasks span creation months; cancelled tasks are excluded by default. Report metadata excludes narrative/snapshot; `GET /workspace/reports/:id` reads the authorized body on demand. `GET /workspace/register` preserves the work-register views and complete counts and returns the selected page plus its limited display references. The UI explicitly fetches all consistent pages before generating a full-scope work summary; it does not export just the visible page.

App starts with the shell. Work register, report list, authorized work and period review use their own queries. Other pages explicitly retain the compatible `/bootstrap` path. Global search queries server-side pages and declares its display limit. Legacy bootstrap remains supported; missing-task historical audit recovery loads the audit collection once and groups it rather than repeatedly scanning it per weekly record.

### Business transfer v6 / storage schema 3

v6 adds strict `taskCommitmentEvents`, `historicalEvidence` and `periodReviewSnapshots`. Full manager exports and task dependency closures carry the relevant history. Raw department snapshots are excluded from member business packets; members use the dedicated authorized review projection instead. v1–v5 remain readable with empty new collections and reject attempts to carry new facts under older format numbers.

Restore checks source references, frozen content hashes, same-version source hashes, audit-backed commitment values, typed nested identities, historical time boundaries, cutoff attribution, unique entries, revision chains, coverage and exact predecessor differences. Derived delivery decisions and weekly compliance are reconstructed using the declared rule version and frozen manifest. Identity mapping updates nested identities and verifiable same-version source hashes before recomputing frozen content hashes; older source versions retain their provenance hashes. Mapping never rewrites evidence prose or formal timestamps. Invalid packets fail atomically; successful writes rotate the operation epoch and do not send notifications.

Runtime `carryWorkflows`, `carryWorkflowRequests` and `periodReviewReceipts` are excluded from business packets and retained in full backups. Their account references also prevent destructive account deletion. Schema 3 adds measured query indexes without rewriting business rows. Unknown future schema versions are rejected; index migration failure rolls back the migration version. Do not run schema 2 binaries against schema 3 databases.

## R2: optional aggregate usage analytics

All paths below are under `/api`, require a currently authorized account and use `Cache-Control: no-store`. Existing same-origin protection applies to writes. These requests use a separate frontend transport: an analytics write never emits a business mutation notification or advances the main database workspace revision.

| Method / path | Contract |
| --- | --- |
| GET `/usage-analytics/status` | `{enabled,version}` for the current account and operation epoch. Disabled for managers, observers, excluded or unusable accounts. No query fields accepted. |
| POST `/usage-analytics/page` | Exactly `{page,version,userId}`. Page must be a fixed enum; version must match the built release and userId the authenticated actor. Invalid/extra fields return 400. Disabled collection is a no-op; accepted requests return 204. |
| GET `/usage-analytics/settings` | Manager only. Settings version, enabled flag, retentionDays, excludedUserIds, effectiveEnabled, activationRequired and buildVersion; storageUnavailable when the optional database failed to open. |
| PUT `/usage-analytics/settings` | Manager only; exactly `{version,enabled,retentionDays,excludedUserIds}`. Optimistic conflict returns 409; retention is 30 or 90 (default 90); IDs must be member accounts. Storage-unavailable mode returns 503 and cannot enable temporary collection. |
| GET `/usage-analytics/summary?from=&to=&version=` | Manager only; optional date/version filters, bounded by retention and the current date. Aggregate counts and distinct members per fixed page/action; current eligible and active member counts, observationDays, observedDays and insufficientData. No individual events returned. |

Collection is disabled until a manager explicitly enables it. Only usable member accounts count; exclusion deletes the account's existing contributions. Page counts deduplicate by Shanghai date, member and release. Successful actions are inferred from fixed business routes and immutable command receipts or saved entity ID/version; failed writes, retries, URL aliases and old receipts from before activation/release do not inflate counts. There is no arbitrary client action endpoint.

The fixed vocabulary is defined in `shared/usage-analytics.ts`. Member rates use distinct eligible members with any accepted event in that interval/release as denominator; the eligible population uses current account status and exclusions. observationDays is the continuous enable window's calendar span for the current release; observedDays counts days with accepted records. The 28-day/5-member insufficientData flag is a sample warning, not evidence of four normal working weeks and not a recommendation to delete functionality.

The sidecar stores a date, release, enum and HMAC-derived member/operation keys, plus manager configuration and its local hashing secret. It never stores business prose, attachments, input payloads, URLs, IP or user-agent data. The sidecar is excluded from business transfer and the normal main-database backup; back it up separately. Changing the main operation epoch makes collection ineffective until explicit reactivation, which clears the old-environment measurements. See [R2 operation and recovery](performance-delivery.md).
