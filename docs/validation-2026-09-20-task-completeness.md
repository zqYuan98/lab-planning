# 工作事项完整性修复与正式部署

2026-09-20 21:19（北京时间），已按用户“修改并且正式部署”的指令上线至 https://lab.notvitamin.com 。

发布标识：`task-completeness-20260920T130005Z`。前一正式版本：`followup-status-20260920T095638Z`。

## 实际行为

- 月度状态统一显示“已发布”，导入来源继续单独标识。它不表示任务已完成，交付和验收状态另行展示。
- 导入明确列出候选、实际事项、已落地、未选择未导入、待处理；历史第三项未选择时显示“未导入（未选择）”，不再误报“已处理”。
- 新文件导入须打开原资料、逐项核对事项数并保存确认，随后才能正式写入。未选真实工作仍计入原文事项；有理由的重复或非任务候选可以从事项数排除。修改候选和重新识别会使原确认失效。计数配合人工逐项核对使用，不能自动证明语义完整。
- 支持人工补录，包括 AI 没有返回任何候选时；保留原始位置、内容和逐行选择审计。AI 不能决定取消勾选、系统关联及整个任务完成。表格默认全选工作表，保存并恢复解析选项；重新识别重新调用模型，失败续跑可复用已完成片段。
- 月目标保留明确交办来源、交办人、日期和协作关系。周 Task 仍为个人任务，协作关系在月目标中设置，无法支持的字段明确提示而不静默丢弃。引用已有 Task 时采用其来源，不能通过导入隐式改变来源或结束整个任务。
- 周阶段“完成”默认不结束整个任务；需明确确认全部完成并填写说明。旧导入已完成但缺整体说明的任务保留原库状态，在“完成待核对”及在手清单提供核对入口。
- 个人清单包含本人未完成 Task，以及本人负责、未验收、未拆本人任务的月目标；目标带真实审批状态，也进入待安排和汇报。建立真实 Task 后按关联去重，可继续沿用该任务安排周工作。管理者本人同样有入口。
- 来源明确值优先，可靠的非导入旧下发记录兼容识别；导入录入身份不作为领导交办证据。缺少明确来源的工作进入“来源待核对”，不擅自猜测。

## 验证结果

- 封存部署包在 Linux、Node 24.20.0 下运行完整测试：**498 / 498 通过**，无跳过。测试前后源码清单哈希一致。
- 最终部署源码 TypeScript 检查通过，Vite 生产构建通过。保留现有较大前端分块提示，无构建失败。
- 独立交叉审查补上旧 Task 空交办字段兼容、成员零可见候选转交管理员、补录协作名单自动匹配和批量周转月清理整体完成字段的边界回归。
- 本地隔离内存库、无外发工作器，浏览器完成：未拆月目标→真实 Task→沿用该 Task 保存周工作；在手数量保持 4、待安排从 4 变 3、本周推进从 0 变 1；本人报告包含目标且不包含他人目标。
- 原三项历史图片副本展示 3 候选、2 落地、1 未导入，并可展开原图。模拟三项文本只有两候选时确认被禁用，补录第三项、保存完整性确认后成功写入三项。
- 桌面和 390px 手机宽度检查通过，页面无横向溢出，浏览器无错误。最终前端资源刷新加载正确。公网登录页渲染正常，无浏览器错误。
- 公网只读 **22 / 22 校验通过**：首页、工作页、个人清单和协作深链接、登录配置、受保护 API、全部 JS/CSS 和 HTML 哈希、新旧功能标识。

## 正式数据与发布边界

发布从当前正式版本组装，只合入本轮修复相对于初始工作区基线的差异。保留工作区未发布的周计划审核、删除等独立改动；这些改动没有进入本次发布。包管理依赖、存储及数据库迁移代码与前一正式版本逐字节相同。

使用固定主机指纹和已有设备身份连接；部署锁和备份锁保护切换，维护期间停止服务后备份并检查 SQLite。切换前后 **802 条受保护实体逐条一致**；仅允许原有运行状态行的连接字段和时间更新。正式环境与 Nginx 配置完全保留，无数据库恢复或业务数据补写。

备份：`/var/backups/lab-planning/pre-task-completeness-20260920T130005Z.sqlite`，完整性 `ok`，SHA256 `50922b4c670397ad0a84db1da2a5843d77645f4202eb29d120754d040c8be19b`。

上线后：服务 `active/running`，重启计数 0，数据库版本 2、完整性 `ok`。16 条月目标、25 条 Task、30 条周记录、6 个账号、5 条外部身份关联与上线前一致。已有通知投递仍为 18 条已送达、11 条跳过，无待发送或处理中队列。

按新清单算法只读投影当时的正式数据，5 位在用成员的 **13 项未完成任务与 6 项未拆解月目标全部覆盖**，未混入他人责任。这 19 项当时均缺少明确来源，进入来源待核对；不存在需要整体完成核对的旧导入 Task。它们是全员汇总，不是某一个人的数量。

现有 4 个导入批次共 20 个候选、19 个写入结果、1 个未选择且无写入结果。未选择的历史事项已可见，但其是否应补录、是否被另一个任务覆盖仍需结合当前工作核对；本次没有擅自更改历史选择或重建任务。

## 留存证据

目录：`output/task-completeness-implementation-20260920/`。

- `baseline/manifest.json`、`assembly.json`、`integration-resolutions.json`：工作区起点及部署包差异来源。
- `release.json`、`release-stage/release-files.sha256`、`release-stage/dist-files.sha256`：309 个源码文件与产物封存清单；压缩包 SHA256 `2ebb070db80bb8075d14180a920a5e12723da4006aaf679268fcbaa1f85908a1`。
- `linux-tests.log`、`register-desktop.png`、`register-mobile.png`、`import-history.png`：测试及页面证据。
- `preflight.json`、`prepared.json`、`activation.json`、`postflight.json`、`live-coverage.json`、`public-verification.json`：正式发布、数据和公网校验。
- `prepare.py`、`activate.py`、`release_helpers.py`：本次可复查的发布流程。失败时仅切回旧代码，绝不以旧库覆盖后续数据。

使用时刷新页面即可加载新版本。可先打开“我的工作清单”的“待安排”和“来源待核对”，再到历史导入批次查看未导入项。未来导入按原文逐项核对，未导入项必须写明理由。
