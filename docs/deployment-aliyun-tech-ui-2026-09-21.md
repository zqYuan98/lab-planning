# 天枢实验室科技风界面发布记录

状态：**2026-09-21 14:53:29（北京时间）已完成上线**，14:54 公网和运行检查通过。

- 正式入口：https://lab.notvitamin.com/
- 本次版本：`tech-ui-20260921T064409Z`。
- 前一版本与代码回退目标：`feedback-20260921T013648Z`。
- 证据目录：`output/tech-ui-deploy-20260921/`。

## 范围与来源

按用户提供的科技风参考图重做登录入口，提供数字地球、网络网格、蓝青玻璃表单、深浅主题切换、密码显示与管理员重设密码提示。内部工作区采用深蓝导航、蓝青选中项、浅蓝灰页面与白色内容区，手机抽屉同步适配。

从前一正式版本的封存目录组装，先验证全部 339 个源文件哈希和前版归档。新包仅修改 5 个 UI 文件，新增 7 个 UI、图形与说明文件，共 346 个源码文件。服务端、共享类型、测试、依赖、部署配置、脚本及数据库迁移均与前版一致。工作区未发布的周计划审核和留痕删除功能未混入本次发布。

发布包 SHA-256：`61973c3ba1917837b9d4df6db8c064878e36d0d8beb833142945e33571b852d9`。

## 发布包验证

- 本机 WSL Ubuntu，官方 Node.js 24.20.0：546/546 现有测试通过；TypeScript 与 Vite 生产构建通过；构建前后封存源码哈希一致。
- 5/5 部署辅助测试在本机 Linux 管理员上下文通过，覆盖独立备份、业务数据一致性、运行中任务阻止切换及配置处理。首次普通用户运行备份辅助测试因 root 文件属主约束失败，使用与实际部署相同的管理员上下文后通过，未放宽生产约束。
- 主 JS 小于 900 kB；所有静态资源低于既有健康检查的 2 MiB 单资源读取上限。Vite 仍提示主包超过建议的 500 kB。
- 远端准备阶段已验证归档、源文件与构建产物哈希，并再次逐文件对比后端、共享代码、测试、脚本和代理示例。复用前版依赖，生产服务器没有执行安装、构建或完整测试。
- 封存产物在本机内存数据库与生产 CSP 下 8/8 浏览器场景通过：桌面与手机、深浅切换及持久化、密码显隐、登录、工作区配色、移动导航。应用运行异常与 CSP 违规均为 0，合成账号仅存在于内存库。
- 公网 29/29 检查通过：入口和深链、鉴权、定位编号、全部 JS/CSS/SVG 哈希、旧功能与本次版本标识。
- 实际公网浏览器只发送匿名 GET，桌面 1440px、手机 390px 首页与反馈深链正常渲染，没有应用运行异常或创建登录会话；页面背景 SVG 和引用的 JS/CSS 均与发布包一致。Cloudflare 原有统计脚本仍被既有 CSP 阻止，已记录并保留原拦截策略，未放宽 CSP。

## 数据与切换原则

继续使用 `/var/lib/lab-planning/lab-planning.sqlite`、既有账号和环境配置。部署持有发布锁和备份锁，临时维护并等待旧入口请求排空，服务正常停止后备份，再切换版本。新服务核对全部受保护实体，仅允许既有 Stream 运行状态字段更新。恢复原始 Nginx 配置字节；失败时回退代码与入口，不用旧库覆盖业务数据。

## 激活结果、备份与回退

- 当前软链接：`/srv/lab-planning/current` → `/srv/lab-planning/releases/tech-ui-20260921T064409Z`。
- 备份：`/var/backups/lab-planning/pre-tech-ui-20260921T064409Z.sqlite`，完整性 `ok`，权限 `0600`。
- 备份 SHA-256：`da479900b9cd983f34c0f462e93ea910c6e04822df1c22173823d6602e8a9946`。
- 切换前后 839 条受保护实体逐条一致；6 个账号、16 个月目标、29 个任务、30 条周记录、5 个外部身份绑定保留。存储版本仍为 2。仅对既有 Stream 运行状态的指定连接字段豁免字节一致性校验。
- 环境文件和 Nginx 配置逐字节保留，未启用新渠道或发送测试通知，未下载正式数据。
- 应用、Nginx、Cloudflare 隧道、备份 timer 均 active；自动重启 0、激活后错误级别日志 0。验收时应用占用约 146 MiB，主机可用内存约 1001 MiB，swap 使用 0。
- 回退目标为 `feedback-20260921T013648Z`，继续使用当前持久数据库。失败自动回退路径已准备，本次未触发；禁止用上线前备份覆盖后续真实业务。

完整证据包括 `assembly.json`、`release.json`、`linux-tests.log`、`linux-build.log`、`browser-tech-ui-results.json`、`preflight.json`、`prepared.json`、`activation.json`、`postflight.json`、`public-verification.json` 与 `browser-public-readonly-results.json`。
