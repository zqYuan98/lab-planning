# 验证记录

验证日期：2026-09-08（Asia/Shanghai）。下文保留首版验收记录；最新一轮系统审查共50项测试通过，Node.js 24.20.0 生产构建、Linux容器运行、在线备份与重启持久化均已通过。完整问题和边界见 [系统审查记录](system-audit-2026-09-08.md)，执行证据见 [GitHub Actions](https://github.com/zqYuan98/lab-planning/actions/runs/34230218993)。

## 自动测试与构建

| 命令 | 实际结果 |
| --- | --- |
| `npm test` | 23 项测试全部通过；0 失败、0 跳过、0 取消 |
| `npm run build` | TypeScript 检查通过，Vite 生产构建成功；处理 1591 个模块 |

运行环境为 Node.js 24.14.0，生产构建使用 Vite 7.3.6。生成的主 JavaScript 约 283.38 kB（gzip 86.50 kB），CSS 约 39.72 kB（gzip 9.16 kB）。Node 在使用原生 SQLite 时输出实验性功能提示，不影响本轮测试结果。

测试使用内存 SQLite 或专门创建的临时数据库，没有读取、导入或改写旧公司数据库。

## 已通过的自动验证范围

| 文件 | 数量 | 覆盖内容 |
| --- | --- | --- |
| `tests/api.test.ts` | 2 | 首次管理者初始化、跨站与非 JSON 请求拒绝、scrypt 密码和安全用户响应、会话过期/退出/密码修改/停用撤销、成员权限、身份伪造、临时任务绕过、证据协议、周状态和版本冲突 |
| `tests/domain.test.ts` | 7 | 月提报退回重提、提交与发布历史、发布后原因和新快照、周正式提交门槛、跨周任务身份和跨月关联、旧草稿修复审计、最后管理者保护、独立年度目标、项目归档、事务回滚、合并责任和来源、周月相交、历史参与权限隔离 |
| `tests/reports.test.ts` | 12 | 全量统计独立于 TOP 重点、报告冻结和重新生成版本、年度进展独立、定稿与冲突、无分母显示、上海时区定时幂等及重启持久性、月报周期、用户脱敏、Word OOXML 表格/重复表头/页码、合并来源排除、临时记录补关联表述、历史月归属与协调信息导出 |
| `tests/workflow.test.ts` | 1 | 真实 HTTP：初始化与成员登录、提报、退回重提、审核发布、周成果与证据、跨周承接、报告保存和定稿、源事实修改后旧报告不变、独立月度成果确认 |
| `tests/backup.test.ts` | 1 | SQLite 在线备份能够独立重新打开，拒绝覆盖已有备份文件 |

## 已完成的独立代码审查

- 报告统计从快照内全量已发布月计划、已提交周记录计算，不由正文或重点选择改变；成员周自报与管理者月度验收分别计算。
- 报告保存固定源数据，编辑仅修改报告正文与标题；定稿和并发修改使用版本检查。AI 请求结束后仍须重新通过权限、版本与可编辑状态检查才能保存。
- Word 导出使用真实文档表格、重复表头和页码。自动测试验证 OOXML 内容与结构；这不等同于在 Word 或渲染后的页面中完成视觉验收。
- 历史计划参与关系不再授权读取新负责人的后续个人任务和草稿；任务、周记录和发布快照分别按当前参与及自身历史归属过滤。
- 新周记录（含草稿）须与关联月相交；任务调整关联时仅修复符合目标月的未提交草稿并审计，已提交周记录保持原月归属。

## 浏览器验收

使用独立数据库 `output/browser-verification.sqlite` 和虚构验证账号；生产初始化数据库不含这些测试数据。

- agent-browser / Chrome 实际操作通过：创建首位管理员、添加成员、成员登录、填写并提交月度提报、管理者审核发布、成员关联已发布月计划建立个人周任务、记录实际成果与证据、成员提交月成果、管理者验收。
- 报告页通过：生成周报、编辑保存、未保存时切换页面出现保护弹窗、继续编辑保留内容、确认定稿后编辑锁定。生产页面重新登录能读取已保存周报和月报。
- 1264×625 短屏已修复并验证退出按钮可用；390×844 手机页面检查了周计划、执行状态、实际成果、下一步和证据链接，未发现内容遮挡。
- 开发页面无浏览器运行错误；生产构建页面的浏览器错误和控制台均为空。
- Word 导出 HTTP 返回正确文档。agent-browser 下载驱动在本机报告“Download was canceled”，改用独立 Playwright / Chrome、启用下载后实际点击相同链接成功，文件名为中文月报名；该驱动差异未作为应用通过依据。

页面截图与本地导出验收材料保存于被 Git 忽略的 `output/`。

停止隔离开发服务后，以 `npm start` 重新启动同一数据库，实际登录读取到原有 2 份报告、1 项月计划和1条周记录，持久性检查通过。随后停止隔离实例，使用默认 `data/lab-planning.sqlite` 启动本机正式构建页面，首位管理员初始化页检查通过；试用库初始为空。

## Word 实际排版验收

以同一批隔离业务数据的周报和月报为样本，使用独立、隐藏的 Microsoft Word 实例导出 PDF，使用 pypdfium2 渲染并逐页查看。修复长证据 URL 挤压列宽和 Markdown 空行造成的多余空段后，周报为 2 页、月报为 3 页；所有页的中文、表格边界、链接换行和页码检查通过。完整风险协调表和待确认事项保留，没有仅一条提示的空白尾页。此项为样本验收，不代表已覆盖任意长度的全部报告数据组合。

## 验证范围边界

- 退回重提、合并、跨月承接、版本冲突和越权通过领域/真实 HTTP 自动测试；本轮未逐一重复其所有浏览器组合。
- 尚未执行实际 AI 服务调用、容器运行、外网部署或多人压力测试。AI 润色需要配置，规则报告生成不依赖 AI。


## 2026-09-11：个人权限与周五提报

本轮更新替代上文旧版“成员提报月计划”的工作流：管理员维护团队目标，成员关联个人任务。权限自动测试覆盖共享目标、历史参与、连续跨月合并来源脱敏、批次分配变化后的编辑/提交与导出隔离。

新增周提报测试覆盖北京时间周五16:00边界、本周与下周两项、整份草稿选择、来源版本冲突、幂等重试、补交缺交留痕、名单冻结与待核对、代录/豁免/更正、重启追补和规则暂停恢复。报告测试验证冻结快照及交付统计不受提报状态影响；业务迁移验证 v2 往返、旧 v1 兼容和成员范围。

使用 127.0.0.1:4327 隔离内存数据库及虚构账号完成真实浏览器操作：成员关联月度目标创建个人任务、本周完成情况正式提交、下周草稿纳入整份提交、逾期状态；管理员按缺交与类型联合筛选、填写原因豁免、注明原因代录下周计划。普通成员页面未出现另一成员的个人任务。豁免后应交项由4项变为3项，截止未交事实保留。测试材料位于被 Git 忽略的 test-results/。

本轮只验证并保存本地实现，未部署生产环境；日计划未纳入此次范围。


最终验证：`npm test` **154/154 通过**；`npm run build` 类型检查与生产构建通过；浏览器运行错误为空。规格与代码质量独立复核通过。迁移新增回归确认：正常启动后的未使用默认规则可预览替换、有业务事实时拒绝替换、预览后变化会阻止恢复、失败事务回滚、再次导出保留审计、非标准周提报时间被拒绝且不改变按时事实。

## 2026-09-12：正式上线验证

用户授权上线后，37服务器已部署应用提交 `2e3fc12`。新镜像隔离154项测试、页面/API/Word/重启持久化通过；正式服务健康，原数据哈希一致，4个有效成员的个人范围只读核验通过。规则从9月14日当周生效，首次截止9月18日16:00。日计划未加入。备份与验证细节见[上线记录](deployment-37-weekly-submissions-2026-09-12.md)。

## 2026-09-14：周提报入口与工作记录关联修复

修复内网HTTP缺少randomUUID造成提报弹窗失败、单条提交文案混淆、已填写内容在管理员状态表不可见，以及查看/编辑/整份核对的周次和成员衔接。157项测试及生产构建通过，使用真实非安全HTTP源验证成员与管理员流程，并部署 `b167ef0`；原数据哈希完整保留。详见[问题与上线记录](weekly-submission-flow-2026-09-14.md)。此轮补足此前localhost验收未覆盖的安全上下文差异。
