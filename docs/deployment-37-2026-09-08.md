# 37服务器部署记录 · 2026-09-08

## 访问与运行状态

- 内网地址：**http://192.168.0.37:4310**，使用原有账号和密码登录。
- 主机：`192.168.0.37`；维护身份：`yzq`；时区：`Asia/Shanghai`。
- Docker Engine `28.5.1`、Compose `v2.40.0`；Docker 服务已配置开机启动。
- 应用容器：`lab-planning-intranet-app-1`，生产模式，健康检查通过。
- 端口仅发布在 `192.168.0.37:4310`；直接 HTTP，无 Nginx 或外部域名。
- 网页、API、Word 导出与草稿检查由同一 Node.js `24.20.0` 进程提供。
- `restart: unless-stopped`、15秒停止宽限及日志轮转已配置。异常健康状态仍需运维处理；健康检查本身不会重启尚存活的进程。

本机原预览已停止，以37为日常写入入口，避免两份数据继续分叉。自动报告草稿保持原设置关闭，AI 保持未配置，没有开启外部数据发送。

## 位置与版本

| 内容 | 位置或标识 |
|---|---|
| 部署根目录 | `/home/yzq/apps/lab-planning` |
| 当前代码包 | `releases/8c7876a/`；`current` 软链接指向此目录 |
| 应用部署提交 | `8c7876a`（包含业务修复及独立内网配置） |
| 运行镜像 | `lab-planning:intranet`，另保留 `lab-planning:8c7876a` 标签 |
| 镜像内容摘要 | `sha256:cb342c2049d9e0dd6bf362731ef6c80e0d5c49e2ada55e4f4dcfbbd7db57a48b` |
| 环境配置 | 部署根 `.env.intranet`，权限600 |
| 数据卷 | `lab-planning-37-data`，容器内 `/app/data` |
| 数据库 | `/app/data/lab-planning.sqlite` |
| 首次迁移备份 | 部署根 `migration/server37-cutover-20260908.sqlite` |
| 服务器备份目录 | 部署根 `backups/`，另有数据卷内 `/app/data/backups/` |
| 主机备份脚本 | 部署根 `ops/backup-intranet.sh`，来自仓库 `scripts/backup-intranet.sh` |

代码通过有校验和的源码归档包上传并在服务器构建；没有复制 GitHub 登录凭据。主机运维脚本独立存放在 `ops/`，后续文档或运维脚本提交不代表重新构建了上述运行镜像。

## 数据迁移与验收

1. 停止本机预览写入，使用 SQLite 在线备份生成最终快照，原库与备份均保留。
2. 通过 SSH 上传，确认两端备份 SHA-256 一致。将备份复制到新的持久卷、验证完整性和管理员存在后启动；没有向网络暴露未初始化的新库。
3. 迁移库的账号、会话、审计和设置逐行摘要与源快照一致，数据库完整性为 `ok`，账号和口令未重设。本次源库尚无项目、计划或报告业务记录；验收记录没有导入正式库。
4. 37上的隔离容器验证通过：管理员初始化和登录、项目/月报写入、静态资源及CSP、Word导出、在线备份、容器重启后持久化。该测试没有开放端口，结束后删除了本次测试容器及其临时卷。
5. 从用户电脑通过内网访问正式地址成功，初始化状态保持 `true`；浏览器登录页面加载正常，没有控制台错误。正式账号由用户使用原密码登录，未为验收创建替代账号或重设密码。
6. 正式容器完成重启并恢复 `healthy`；生产数据卷保持原挂载。首次服务器备份下载回本机后，通过数据库完整性校验。

迁移时，WAL格式备份在只读挂载点无法创建SQLite辅助文件，故先复制至持久卷上的独立候选文件校验，再发布为正式库；上传备份保持不变。候选文件与正式路径均检查不存在后才创建。

## 自动备份

- `yzq` crontab：每天 **03:10，Asia/Shanghai**。
- 调用 `/usr/bin/bash /home/yzq/apps/lab-planning/ops/backup-intranet.sh`。
- 日志：`/home/yzq/apps/lab-planning/backups/backup-cron.log`。
- 备份脚本已在精简环境中实际执行成功，数据库在线备份校验后复制到宿主机，唯一文件名、并发锁及不覆盖既有文件保护生效。
- 修改定时任务前保留了原 crontab，安装时检测并发修改，未移除原任务。
- 目前自动备份仍在37同机，不自动删除，也没有配置持续异机复制。首次迁移备份和首份服务器备份在原电脑另存一份；后续需要运维安排长期异机备份和保留周期。

## 常用维护命令

```sh
cd /home/yzq/apps/lab-planning
docker compose --env-file .env.intranet -f current/compose.intranet.yaml ps
docker compose --env-file .env.intranet -f current/compose.intranet.yaml logs --tail=100 app
/usr/bin/bash /home/yzq/apps/lab-planning/ops/backup-intranet.sh
```

更新、迁移和新卷恢复见[内网部署手册](intranet-deployment.md)。不要在更新时删除持久卷；HTTP地址用于可信内网，不应转发到公网。
