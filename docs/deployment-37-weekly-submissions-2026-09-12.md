# 37 服务器：个人任务权限与周五提报上线

2026-09-12 10:59（Asia/Shanghai），按用户“日计划暂不加，把更新上线”的授权部署。

- 地址：http://192.168.0.37:4310/
- 应用提交：`2e3fc12`，分支 `codex/personal-weekly-submissions`。
- 当前目录：`/home/yzq/apps/lab-planning/releases/2e3fc12`。
- 镜像：`lab-planning:2e3fc12`，运行别名 `lab-planning:intranet`。
- 镜像 ID：`sha256:b377e405e5acb3e4804013720bcf8aaab242a8ef7e9b924edb5e3e7fd533b5c9`。
- 沿用 `/home/yzq/apps/lab-planning/.env.intranet` 和 `lab-planning-37-data`，没有替换正式库或初始化账号。

## 上线行为

管理员维护团队月度目标，成员直接关联目标建立个人任务；普通成员只看自己的个人任务与周记录。每周五北京时间16:00前分别正式提交本周完成情况和下周计划，保留缺交、补交、修订、代录、豁免与更正记录。日计划未加入。

正式库首次规则从 **2026-09-14 当周**生效，首次截止为 **2026-09-18 16:00**，对应9月14日当周完成情况及9月21日当周计划。首次上线没有追记历史缺交。

## 备份与数据核验

- 在线备份：`/home/yzq/apps/lab-planning/backups/lab-planning-20260912T025900.167674954Z-1863126.sqlite`。
- 停止旧服务后的最终备份：`/home/yzq/apps/lab-planning/backups/before-weekly-2e3fc12.sqlite`；卷内也保留副本，完整性检查通过。
- 切换前后全部原实体（含账号、会话、业务及配置）的排序哈希相同：`464b475fa4f9a2ce03958e99c49fc2206eb3bc39827a9e245725c730325165e4`。新功能6个集合单独统计，首次只新增1条周提报规则。
- SQLite `integrity_check=ok`。原6个账号、11个月度目标、6个个人任务、6条周记录及其他原实体保留。
- 原每日03:10备份任务保持有效。

## 验证

- 服务器成功构建生产镜像；该镜像隔离运行 **154/154 测试通过**，未挂载正式数据卷。
- 独立容器完成页面/资源、合成账号登录、SQLite写入、Word导出及重启持久化验证；验收容器与匿名卷已清理。
- 正式容器 `running / healthy`，认证状态为已初始化，启动日志无异常。
- 正式数据库只读适配器调用实际 Domain 投影，4个有效普通成员均只返回自己的任务/周记录，无部门报告；未创建正式测试账号、会话或业务数据。
- 真实浏览器打开正式登录页成功，加载的新资源包含周五提报及关联个人任务界面；浏览器运行错误为空。
- 未登录请求 bootstrap、周提报、业务导出均返回401。

构建、测试、切换与校验文件保存在服务器 `ops/`，名称包含 `2e3fc12`。本次没有使用真实账号密码执行生产写入；成员与管理员完整操作已在隔离浏览器和HTTP测试中验证。

## 保留版本与恢复边界

原目录：`/home/yzq/apps/lab-planning/releases/a746a1fc7510`。
原镜像：`lab-planning:before-weekly-2e3fc12`，ID `sha256:8998329d977107ccd96eb98a14526580894ad83a3c54b8c3b1b64cc3e8165171`。

旧版本包含此次修复的成员可见范围问题，也不执行新周提报规则，后续故障优先发布兼容修复。不得用上线前备份直接覆盖已经产生新数据的正式库；恢复应保全现有卷并单独验证。
