# Word 周报智能体发布记录

状态：**2026-09-21 18:44:57（北京时间）已上线**，18:45–18:46 公网、运行与浏览器检查通过。

- 正式入口：[报告中心](https://lab.notvitamin.com/work?view=reports)，使用管理者账号打开「周报模板与生成」。
- 本次版本：`weekly-agent-20260921T103834Z`；前一版本：`weekly-review-20260921T080122Z`。
- 功能提交：`6a38e98`，部署配置补充：`648519d`。
- 证据目录：`output/weekly-agent-deploy-20260921/`。

## 发布范围

上传公司 Word 模板和历史周报范例、确认模板字段与写作规则、试填版式核对、冻结月度目标及周执行事实、生成可编辑草稿、来源校验、分节改写、Word 预览、定稿文件归档与导出。包含作业取消/重试、草稿恢复、每周自动生成与遗漏处理、业务 JSON v4 导出恢复。

首次使用应先上传模板、确认映射、下载试填文件并用 Word 核对版式，再启用模板。**自动周报默认关闭**；没有上传本机样本至生产，没有调用生产模型，没有发送测试通知。AI 沿用已有服务端配置，未配置时仍可生成规则草稿；月报模板与独立分析报告属于后续阶段。

发布包从当前线上封存源码组装，共 386 个源文件，19 个修改、30 个新增，337 个其余文件与生产基线字节一致。只从已提交周报改动取代码，保留原有周计划审核、反馈与响应式界面；其他任务尚未发布的界面修改未进入本包。

## 验证

- 实际发布包在 WSL Ubuntu、Node.js 24.20.0 下 **654/654 测试通过**；TypeScript 和 Vite 生产构建通过，构建前后源码哈希一致。既有主包超过 Vite 500 kB 建议阈值的提示仍存在。
- Linux 依赖单独封装，服务器核对 21,309 个普通文件和 24 个相对符号链接。没有在服务器安装依赖、构建或运行整套测试。
- 部署数据与代理守卫 **14/14 通过**。首次启动只允许新增结构严格匹配、默认关闭的 `settings/report-agent-schedule`；新报告、资产、作业或修改后的业务记录都会阻止自动退回旧代码。
- 公网放行前，经仅本机可访问的候选代理端口验证 **11 项请求大小与匿名鉴权边界**。Word 资产为 18 MiB，报告/模板编辑为 4 MiB；既有恢复 35 MiB、导入 16 MiB、反馈 9 MiB 和普通请求 256 KiB 配置保留。
- 公网 **39/39 发布检查、9/9 周报匿名权限检查通过**，验证发布版本、JS/CSS/SVG 哈希、所有周报按需加载模块及现有功能标识。服务器同时核对全部 dist 文件，包括图标。
- 浏览器匿名访问报告中心深链，1280px 和 390px 均显示正常登录页，无横向溢出，无 Cookie；验收后关闭专用浏览器。未登录或提交生产业务表单。既有 Cloudflare 统计脚本仍被 CSP 拦截，未放宽策略；未登录的 `/api/auth/me` 返回预期 401。
- 原 Word 样本版式、编辑、定稿和备份恢复的本地功能验收见[实施记录](validation-2026-09-21-weekly-report-agent.md)。生产检查不生成虚构业务数据。

## 数据保护与运行状态

部署持有发布锁和备份锁，公网进入维护后排空旧请求，以 45 秒停止宽限正常关闭旧进程，完成独立 SQLite 备份，再切换软链接。新进程与内部代理验证通过、业务逐项比对通过后恢复公网。

- 保护的 **871 条原有实体保持不变**；仅允许 Stream 连接状态的指定运行字段变化，新增一条默认关闭的周报调度设置。
- 6 个账号、16 个月度目标、29 个任务、31 条周记录、7 份历史提报、5 个外部身份绑定保留。
- 数据库仍为 `/var/lib/lab-planning/lab-planning.sqlite`，完整性 `ok`，存储结构版本仍为 2。没有下载生产数据库，没有执行数据库恢复。
- 上线前备份：`/var/backups/lab-planning/pre-weekly-agent-20260921T103834Z.sqlite`，完整性 `ok`。
- 备份 SHA-256：`f4cb7b3f6a07db79dedef75545e66bd0e76641539dcb41c3dc8d238b8ec28945`。
- 环境文件字节不变；Nginx 只增加两项周报路径配置，所有原有配置字节保留。
- 应用、Nginx、Cloudflare 隧道和备份 timer 均 active；检查时自动重启 0、激活后错误级别日志 0，应用约 165 MiB，系统可用内存约 965 MiB，swap 使用 0。

源码/dist 归档 SHA-256：`14c13e9cc144c27ff03a53bb71dae75c0c05028c828b7c4f4f42bb6b962e556d`。

Linux 依赖归档 SHA-256：`b6ee2319881cb1230dc3f4f8de7f8d727144c899e818da08d79a05b793b8aad1`。

## 回退边界

本次发布成功，未触发回退。上线后只要已经创建模板、资产或智能体报告，旧代码的编辑及导出逻辑就不能完整处理新记录，应修复并继续发布兼容新版，不能直接切回旧版，也不能以发布前备份覆盖后续真实业务。

部署脚本只在重新进入维护、排空请求并停机后，确认所有原业务未变且最多新增一个默认关闭设置时才允许退回旧代码；不会恢复旧数据库覆盖现库。部署阶段脚本位于证据目录，`init-release-scripts.py` 为生成器，实际审阅和执行的是最终 `prepare.py`、`activate.py`、`weekly_release_checks.py`，不可重复执行已完成的发布。

完整证据见 `release.json`、`artifact-verification.json`、`assembly.json`、`linux-tests.log`、`linux-build.log`、`deployment-guard-tests.log`、`preflight.json`、`prepared.json`、`activation.json`、`postflight.json`、`public-verification.json`、`weekly-public-smoke.json` 和 `browser-public-verification.json`。
